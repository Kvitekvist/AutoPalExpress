# Ticket Memory

This file provides a quick overview of completed work.

Append entries only.

---

## Completed Tickets

All work before TICKET-0001 was done directly through conversation and recorded in `memory/architecture.md`, `memory/decisions.md`, and `memory/project_memory.md` instead of numbered tickets - don't retroactively invent ticket numbers for that untracked work, since that would create a false impression of a history that didn't actually happen this way. The ticket system starts fresh from TICKET-0001 below.

* **TICKET-0001** (2026-07-06, Bug) - Server could launch on a stale port instead of the one actually configured, because `instances.json`'s stored `gamePort` and the live `PalWorldSettings.ini`'s `PublicPort` could disagree with nothing reconciling them. World Settings' "Game Port" field is now the single place the port is edited; starting a server enforces that port into the ini (self-healing if missing) and syncs it back into `instances.json`.
* **TICKET-0002** (2026-07-06, Bug) - `palworld_settings._get_field`/`_set_field` couldn't see a key that was the first field in `OptionSettings=(...)`, found while building TICKET-0001's self-heal path. Fixed the lookbehind to also accept start-of-string.
* **TICKET-0003** (2026-07-06, Bug) - Neither `RemoteAccessPanel` nor `PortForwardPanel` could show or remove a UPnP port mapping unless *that browser session* had just created it - `forwarded` was local-only React state, never derived from the router's actual mapping. Added `upnp.get_port_mapping()` (`GetSpecificPortMappingEntry`) and exposed real mapping state (including which machine currently holds it) via `/network/upnp/status`, so a mapping from another machine or an earlier session can now be seen and removed - the actual problem reported (router's own UI couldn't remove a UPnP-added entry, and neither could this tool until now).
* **TICKET-0004** (2026-07-06, Enhancement) - Follow-up UI reorg after reviewing the TICKET-0003 build: moved the active server's disk path from Dashboard (visible to all admins) to Super Admin (role-gated); removed the "Game Port" field from World Settings and made Super Admin's "Share With Friends" port field the one and only place to view/edit/save a server's actual port; removed `ServerConnectionPanel` and its mock API entirely (confirmed with the user it was dead, non-functional leftover UI with no real backend, not something to relocate).
* **TICKET-0005** (2026-07-06, Bug) - TICKET-0003's "Remove This Forward" button only showed when the router's `GetSpecificPortMappingEntry` query succeeded - a far less reliably-supported UPnP action than Add/Delete across consumer routers, so it silently never appeared for the user. Made "Forward" and "Remove" both unconditionally available whenever a UPnP router is present, since removal already safely no-ops (UPnP 714) if there's nothing mapped - no condition on detection needed at all.
* **TICKET-0007** (2026-07-06, Bug) - Dashboard CPU% didn't match Task Manager - `psutil.Process.cpu_percent()` is relative to one core, not normalized across all logical cores like Task Manager, so it read inflated by the core count on any multi-core machine. Fixed by dividing by `psutil.cpu_count()` in `process_manager._tree_cpu_ram()`. RAM reporting was checked and found already correct.
* **TICKET-0006** (2026-07-06, Bug) - Kicking a player completed without error but didn't disconnect them. Two real bugs fixed: `show_players()` wasn't stripping whitespace from `ShowPlayers` CSV fields; and, found from the user's own server console log (`User id: steam_<SteamID64>`), `KickPlayer`/`BanPlayer`/`UnBanPlayer` needed that same `steam_`-prefixed ID form, not the bare numeric ID `ShowPlayers` returns. Confirmed fixed by the user against a real kick.
* **TICKET-0008** (2026-07-06, Bug) - Security audit finding: any authenticated user (not just super admin) could read the real RCON `AdminPassword`/`ServerPassword` in plaintext via `GET /api/server-settings`. Now redacted for non-super-admins; only the super admin can view or change these fields. Verified via a real HTTP request through the app.
* **TICKET-0009** (2026-07-06, Bug) - Security audit finding: any authenticated user could register an arbitrary host folder as a "server instance," override the mods path to any folder, or trigger native folder-picker dialogs on the host desktop. Restricted instance import/deploy/remove and mods-path override/browse to super admin only (matches how comparable game-server panels scope this); `InstanceManagerPanel` UI updated to match. Verified via real HTTP requests (403 for a regular admin).
* **TICKET-0010** (2026-07-06, Bug) - Security audit finding: login response timing revealed whether a username existed (instant for unknown users, ~67ms PBKDF2 cost for real ones). Fixed by always running a dummy PBKDF2 comparison for unknown usernames. Measured before/after: timing ratio went from near-zero to ~1.01.
* **TICKET-0011** (2026-07-06, Bug) - Security audit finding: session tokens never expired server-side (only the client-side cookie had a 30-day expiry). Added a matching server-side max age to `session_store`, verified sessions are correctly rejected and pruned once expired.
* **TICKET-0012** (2026-07-06, Enhancement) - Locked the whole Settings tab behind super admin (nav-hidden + route-guarded, same pattern as Super Admin), following on from TICKET-0009. Moved `Ue4ssPanel` out of Settings and into the Mods page (and its file from `components/settings/` to `components/mods/`) so UE4SS management stays available to regular admins. Simplified `InstanceManagerPanel`'s TICKET-0009 role checks back out since they're now unreachable dead code - the page itself is gated.
* **TICKET-0013** (2026-07-06, Bug) - Fresh security audit (inspired by reviewing Crafty Controller's static file handler): `spa_fallback` in `main.py` concatenated a raw, unauthenticated user path onto the frontend directory with no containment check of its own - not currently exploitable (Starlette normalizes `..` out of the URL before the handler runs) but that's incidental framework behavior, not a guarantee. Added an explicit resolved-path containment check, same pattern as `mod_installer._safe_extract`. Verified by calling the function directly with a raw traversal string, bypassing whatever the HTTP layer normalizes.
* **TICKET-0014** (2026-07-06, Bug) - Same audit: `automation.router` was still `_authed` (any admin) after TICKET-0012 locked its only UI entry point behind super admin. Changed to `_super_admin_only` to match. Verified via a real HTTP request (403 for a regular admin).
* **TICKET-0015** (2026-07-06, Enhancement) - Moved "Install From File" mod upload from a conditionally-hidden button on the Mods page to its own section on Super Admin. Already correctly permission-gated on both ends beforehand (backend `require_super_admin`, frontend role check) - purely an organization change, consolidating high-risk actions onto the one page that's entirely reserved for the super admin instead of scattering hidden conditionals across shared pages.
* **TICKET-0016** (2026-07-06, Enhancement) - Added a shared `ManualForwardInstructions` component (name, protocol, external/internal IP, external/internal port) shown when UPnP isn't available, in both the admin-port and game-port forwarding panels. Added `local_ip` to `/network/upnp/status` so the exact LAN IP to enter is shown, not just a generic "forward it manually" message. Verified end-to-end against the real router/network.
* **TICKET-0017** (2026-07-06, Enhancement) - Removed "(any)" from the External IP row. Removed the "Install Mod" manual-entry feature entirely (backend route, API call, frontend button/dialog) - it only ever created an untracked, non-file-backed mod entry indistinguishable from a real one in the UI, confirmed with the user it had no real value as built.
* **TICKET-0018** (2026-07-06, Feature) - Moved initial setup (super admin account, Nexus API key, an optional first server) into the Windows installer itself via custom Inno Setup wizard pages, applied automatically on first app launch (`app/services/first_run_setup.py`) using the exact same code paths the UI uses. New deployments now go in a fixed `data/servers/<name>` folder instead of a browsed path. Backend thoroughly tested via real HTTP requests; the installer's own interactive GUI click-through still needs a real manual run - this sandboxed shell can't drive a GUI wizard (confirmed even a `/VERYSILENT` install hangs on an unrelated pre-existing privilege dialog, not something this ticket introduced).
* **TICKET-0019** (2026-07-07, Enhancement) - Eliminated both floating console windows (the admin tool's own, and the one Palworld's dedicated server creates for itself). Confirmed live against a real running server that `CREATE_NO_WINDOW` suppresses the entire process tree's window, but that real console *content* can't be piped/captured that way - Palworld writes through its own low-level console API, not stdout, and doesn't write a log file either even with `-log` passed. `process_manager.start()` now launches windowless; the admin backend itself now runs windowed too (`console=False`), with `desktop_app.py` redirecting `sys.stdout`/`sys.stderr` to a real log file first (a windowed PyInstaller build has both as `None`, which would otherwise crash the instant anything logs).
* **TICKET-0020** (2026-07-07, Feature) - Replaced the mocked Logs page with a real activity feed (new `app/services/activity_log.py`, persisted JSONL). Found *why* TICKET-0019 couldn't capture Palworld's console content: `ImGui.ini` in the server's own config confirms it renders its console via Dear ImGui (a GPU overlay), not a real text console - there's no text buffer anywhere to read. Confirmed this with the user (AskUserQuestion) and pivoted to logging real events instead: server start/stop/force-kill, player join/leave (now logged regardless of the `joinLeaveMessages` toggle, which only gates the in-game broadcast), kick/ban/unban (with who and whom), and scheduled backup/restart outcomes.
* **TICKET-0021** (2026-07-07, Enhancement) - Clarified the Nexus Mods browsing setup flow: the Mods page Browse dialog and disconnected Nexus state now explicitly tell users that the super admin must connect a Nexus Mods API key in Super Admin first, and the CTA routes to Super Admin instead of the old Settings location. Updated the Super Admin Nexus panel and README wording to match.
* **TICKET-0022** (2026-07-07, Enhancement) - Refreshed public release docs before community sharing: README and Nexus description now match the current real feature set, no longer claim Logs/Players/World Settings are placeholders, point Nexus setup to Super Admin, and include clearer HTTP/trusted-invite/unsigned-installer notes.
* **TICKET-0023** (2026-07-07, Enhancement) - Restored visible command windows and improved Logs: packaged AutoPalExpress is console-visible again while teeing stdout/stderr to `backend.log`, Palworld launches without `CREATE_NO_WINDOW`, and the Logs page now shows AutoPalExpress output beside the real server activity feed. Palworld's exact CMD text still cannot be mirrored because it is not normal stdout/log-file text.
* **TICKET-0024** (2026-07-07, Enhancement) - Reworked the root GitHub README for non-technical community readers: colorful shields, screenshot placeholders, GitHub alert blocks (`NOTE`, `TIP`, `IMPORTANT`, `WARNING`, `CAUTION`), simpler quick-start/security sections, and collapsible technical details.
* **TICKET-0025** (2026-07-07, Enhancement) - Added the maintainer-provided Dashboard, Mods, Super Admin, and Logs screenshots from `images/` to the GitHub README and committed the image assets.
* **TICKET-0026** (2026-07-07, Bug) - Fixed packaged startup crash after restoring the visible AutoPalExpress console: `_Tee` now exposes stream-like methods/attributes such as `isatty()`, `fileno()`, `encoding`, and `errors`, so Uvicorn's logging formatter can configure normally while stdout/stderr still tee into `backend.log`.
* **TICKET-0027** (2026-07-07, Enhancement) - Updated Nexus integration for public-release compliance after Nexus support pointed to the API Acceptable Use Policy and suggested GraphQL: browsing now uses unauthenticated Nexus GraphQL metadata, verified manual uploads use GraphQL `fileHash`, requests identify AutoPalExpress with app headers, one-click Nexus downloads are paused pending registered app/OAuth approval, and the installer/docs no longer ask public users for a personal Nexus API key.
* **TICKET-0028** (2026-07-08, Enhancement) - Replaced Palworld RCON usage with Palworld's official local REST API. Removed the RCON client, added `palworld_rest.py`, switched players/server-control/scheduler/backups/status metrics to REST, enabled `RESTAPIEnabled`/`RESTAPIPort` before launch, changed user-facing labels/docs to REST API, and kept `rconPort`/`rconReady` only as compatibility field names.
* **TICKET-0029** (2026-07-08, Bug) - Fixed Nexus Browse cards opening the wrong public Nexus URL path and replaced the misleading "Verified File Install" button-like control with the real supported flow: View on Nexus to download, then Super Admin's Install File shortcut/notice for verified upload.
* **TICKET-0030** (2026-07-08, Bug) - Fixed Dashboard CPU/RAM reporting by discovering matching `PalServer.exe` and `PalServer-Win64-Shipping-Cmd.exe` processes inside the selected server folder instead of only sampling the remembered launcher tree. Tick-rate milliseconds now show as unavailable when Palworld REST metrics omit frame time instead of displaying `0 ms`.
* **TICKET-0031** (2026-07-08, Feature) - Added a custom install-location picker for new Palworld server deployments. The deploy wizard and installer first-server flow both default to AutoPalExpress' data `servers` folder but can place the generated per-server folder under a super-admin-selected parent folder.
* **TICKET-0032** (2026-07-08, Feature) - Added a noticeable Settings-page checkbox for showing an instance in Palworld's Community Server list. The option is stored per server and adds the `-publiclobby` launch argument on the next start.
* **TICKET-0033** (2026-07-08, Feature) - Added safe per-server launch-option controls for Palworld's performance flags, optional worker thread count, and JSON log format. Existing single-source settings were intentionally not duplicated: port, player count, community listing, and public IP/port remain in their current flows.
* **TICKET-0034** (2026-07-08, Enhancement) - Moved the safe launch-option controls from Settings to World Settings so performance flags, worker thread override, and JSON log format are easier to find beside the server behavior configuration. The controls remain backed by per-instance storage and still require a restart.
* **TICKET-0035** (2026-07-08, Bug) - Fixed World Settings hiding the Launch Options panel by returning launch-option state in the same `/api/server-settings` response as the visible `.ini` fields, instead of relying on a separate active-instance request.
* **TICKET-0036** (2026-07-08, Enhancement) - Added a dedicated Launcher Flags sidebar page for Community Server listing, performance flags, worker thread override, and JSON log format. Removed those launch-argument controls from Settings and World Settings so the new page is the only visible editor.
* **TICKET-0037** (2026-07-09, Feature) - Added Windows startup recovery. Settings now lets the super admin start AutoPalExpress with Windows and auto-start the active server when the app launches; the installer has the same optional startup-recovery task and explains that it helps bring the server back after machine restarts.
* **TICKET-0038** (2026-07-09, Feature) - Restored direct Nexus installs for super admins with a saved Nexus Premium API key. Nexus Browse now shows an Install button for that path, the backend downloads through Nexus's download-link endpoint, installs with the existing archive safety checks, and keeps verified manual upload as the fallback.
* **TICKET-0039** (2026-07-09, Bug) - Fixed duplicate Server Instances rows by deduplicating instance records by normalized server folder and preventing future duplicate creates for the same path. Settings now has per-server actions for Switch To, Open in Explorer, Remove, and Remove and Delete; the destructive delete path refuses to run while that server is online and then deletes the registered server folder.
* **TICKET-0040** (2026-07-09, Enhancement) - Installer update/repair runs now detect existing `%LOCALAPPDATA%\PalworldServerAdmin\data`, skip first-time server/admin setup pages, delete any stale `first_run_seed.json`, and leave the existing server list and admin account in place. Verified with a successful Inno Setup compile.
* **TICKET-0041** (2026-07-09, Bug / Enhancement) - Follow-up after installed-build verification: Nexus Browse now shows Direct Install for super admins even before a Premium key is connected, with clear requirement text; Launcher Flags is renamed Launcher Options and exposes separate `-useperfthreads`, `-NoAsyncLoadingThread`, `-UseMultithreadForDS`, and `-publiclobby` toggles; server-instance dedupe now canonicalizes stored paths and the folder action is labeled Browse Files.
* **TICKET-0042** (2026-07-09, Bug) - Rebuilt the Windows installer after verifying that source and `web/dist` already contained Launcher Options. Updated the release checksum to `DE47FB088842FA3135856407919601A759B642BFADF328CCED007B7E1EC2042B`.
* **TICKET-0043** (2026-07-09, Feature) - Added Server Control update checks and confirmed updates. AutoPalExpress reads the active server's installed Steam build id, compares it to SteamCMD's current public build id, asks before updating when a newer build is available, and runs the update as a pollable SteamCMD job only while the server is offline.
* **TICKET-0044** (2026-07-09, Feature) - Added Super Admin-only Launcher Options toggles for `-publicip` and `-publicport`. The actual values are read-only and sourced from the same public address/game-port flow used by Super Admin's Share With Friends panel, preserving one place to edit the port.
* **TICKET-0045** (2026-07-09, Bug) - Fixed Launcher Options showing the stale default port for `-publicport` after the live server port had been changed. Instance API views now expose the effective live game port and Launcher Options prefers that value.
* **TICKET-0046** (2026-07-09, Bug) - Fixed remaining port drift by centralizing game-port resolution: a stored custom Super Admin port now survives reinstall/update and is enforced into the ini at launch, while old default records can still adopt a custom ini port.
* **TICKET-0047** (2026-07-09, Bug) - Fixed the Dashboard roster staying empty after the REST migration when Palworld was running its REST API but AutoPalExpress skipped calls because the ini credential check looked incomplete. The REST client now falls back to the stored local management port and normalizes player field names before Dashboard roster and join/leave tracking consume them.
* **TICKET-0048** (2026-07-09, Bug) - Fixed Palworld REST Unauthorized failures when `AdminPassword` was blank. The launch path now preserves the live ini and user-set passwords, but creates a random AdminPassword when the field is missing or empty so REST-backed Dashboard, Players, Server Control, and scheduler calls can authenticate.
* **TICKET-0104** (2026-07-13, Feature) - Added Save Import (Settings > Automation): a host can point a native folder picker at a world save copied over from another PC (e.g. their own Steam co-op save) and it's dropped into the server's `SaveGames/0` slot, backing up whatever was there before via a new `backup_service.backup_before_import()`. No Steam install needed on the server machine, since `SaveGames/0` is a fixed dedicated-server slot name, not tied to a Steam account. Verified with a throwaway fixture run through the real venv (no Palworld install available in this environment) - this caught and fixed a real pre-existing bug where two backups landing in the same second collided on their timestamp-named folder (`_unique_backup_dest()` now falls back to a numbered suffix, used by both `run_backup()` and the new helper).
* **TICKET-0109** (2026-07-13, Feature/Bug) - Two requests: added a "Pending Install" card on the Mods page for wishlisted-but-not-installed mods (new `PendingModCard.tsx`; excludes update requests for an already-installed mod, which keep their own "Update Requested" badge on `ModCard`); and reverted TICKET-0107's disabled "Connect" state - the user explicitly wants "Nexus Login" clickable now even without a confirmed slug, to see the real Nexus authorization step for themselves rather than have it hidden. Removed `SLUG_CONFIRMED`/`is_configured()`/`GET /sso/configured` entirely rather than leaving them as dead code. Rebuilt and shipped (checksum `2F0BAA4E124B14D5C4D26BB769859CFD5A770A1B618889385260C08BEF1EC0FD`). Saved as a standing preference: `[[feedback_show_dont_hide_unconfirmed_integrations]]`.
* **TICKET-0108** (2026-07-13, Bug) - User report: an expected "no Nexus connection" error on Super Admin also force-logged them out of AutoPalExpress entirely instead of showing a normal popup. Traced to `web/src/api/httpClient.ts`'s blanket rule (any 401 outside `/api/auth/*` = dead session, forced logout) colliding with two backend sources of Nexus-related 401s: `nexus_session.require_api_key()`'s literal 401 for "not connected" (now the default state for every super admin after TICKET-0106's legacy-key clear, since real SSO isn't available yet per TICKET-0107) and four route handlers forwarding Nexus's own 401 (invalid/expired key) straight through as AutoPalExpress's own status code. Fixed by remapping both to 400 (`require_api_key` directly; added `NexusApiError.http_status` for the rest, which passes 403/429/etc. through unchanged). Verified directly at the unit level (no interactive browser here). Rebuilt and shipped (checksum `B693CBFBC3C87FE94F9B0604878BD53BCCD898D997D8E17661B8B200A0AD143A`). Broader lesson: 401 has exactly one meaning in this app's frontend contract - never reuse it for a recoverable, non-session condition.
* **TICKET-0107** (2026-07-13, Bug) - Asked the user directly what "fix it" should mean for the remaining gap (no real Nexus application slug yet - only Nexus can assign one). They chose graceful degradation over guessing a value. Added `nexus_sso.SLUG_CONFIRMED` (a separate flag from `APPLICATION_SLUG` on purpose - Nexus's real slug could coincidentally equal the placeholder text, so string-comparing would be wrong) plus `is_configured()`; `POST /sso/start` now 409s instead of starting a doomed session, and `NexusIntegrationPanel` shows a disabled "Pending Nexus Mods Approval" button instead of one that would send the super admin to a broken Nexus page. Rebuilt and shipped (checksum `BF8A8D31377999031F46EC81A6A1FDEFDBF48880681A812AB135CC63351049D7`). To finish once Nexus responds: set the real slug and flip `SLUG_CONFIRMED = True` together, then do one real click-through test.
* **TICKET-0106** (2026-07-13, Bug) - User caught this immediately after updating their live server: mods still installed/wishlisted fine despite having no real Nexus application slug yet and never reconnecting via SSO. Root cause: TICKET-0105 blocked *pasting a new* personal key but never invalidated one already saved before that change shipped - it just kept quietly authenticating downloads, the exact thing Nexus's registration requires removed. Fixed by tagging SSO-obtained connections with `"via": "sso"` and having `nexus_session.get_record()` self-heal any `connected: true` record missing that marker back to disconnected on its very next read - no separate migration script. Verified directly against a `nexus.json` shaped like the old flow's output (gets cleared) and one already carrying `via: sso` (left alone). Rebuilt and shipped in the same installer as TICKET-0105 (checksum `CF2529186E265FBF8E8383B16903EDBCCC8A4670E50FCA3E795D197E055A640C`).
* **TICKET-0105** (2026-07-13, Feature) - Nexus Mods support's final requirement to approve AutoPalExpress's app registration: remove personal API key usage. Researched Nexus's real registered-app mechanism (SSO over `wss://sso.nexusmods.com`, the same one Vortex/MO2 use) and replaced the paste-a-key `NexusIntegrationPanel` flow with it (new `app/services/nexus_sso.py`, `/nexus/sso/start`+`/sso/status/{id}`, `/nexus/connect` removed) - the resulting key is still a per-user Nexus key, just obtained via Nexus's own consent redirect instead of manual copy/paste, which is what Nexus's registration actually gates on. Everything already using a saved key (Direct Install, Wishlist approve) needed zero changes. Also made per-mod `updateAvailable`/`latestVersion` real (previously always `false`/unset - dead fields) via a keyless, batched GraphQL query confirmed live against the real API (`gameId`+`modId` OR-combined sub-filters - discovered live that Nexus's GraphQL has no `IN` operator for a single filter field); the old one-click super-admin "Update" endpoint is gone, replaced by the existing wishlist add/approve pipeline (which already handled "replace an existing `sourceModId` mod" with no changes needed), with an "Update" badge in Mod Wishlist distinguishing it from a fresh install. Uses a placeholder application slug (`nexus_sso.APPLICATION_SLUG`) pending Nexus's confirmation - real end-to-end SSO and the browser UI are both NEEDS MANUAL VERIFICATION (verified instead: the live GraphQL queries via curl, the update-check logic against real mod ids, and the SSO client protocol state machine against a fake in-process websocket covering success/error/abandoned-connection cases).
* **TICKET-0049** (2026-07-09, Release) - Added a release changelog section summarizing the local changes ahead of `origin/main` (TICKET-0041 through TICKET-0048), rebuilt the final executable/installer, and refreshed the release checksum.
* **TICKET-0050** (2026-07-10, Bug) - Fixed launch-time game-port enforcement so it edits the live `PalWorldSettings.ini` in place instead of starting from Palworld's default template, preserving unrelated world settings while still syncing the remembered Super Admin port.
* **TICKET-0051** (2026-07-10, Release) - Rebuilt the packaged executable and installer after the TICKET-0050 ini-preservation fix and refreshed the release checksum.
* **TICKET-0052** (2026-07-10, Feature) - Added a bundled Diagnose AutoPalExpress command for users who cannot tell why a server is not working. The command elevates for firewall inspection, checks the active instance, server folder, `PalWorldSettings.ini`, game/REST ports, local listeners, REST authentication, and Windows Firewall, then saves a support report under `%LOCALAPPDATA%\PalworldServerAdmin\diagnostics`.
* **TICKET-0053** (2026-07-10, Enhancement) - Improved World Settings discoverability by grouping settings under readable section headings, adding tooltips to every field, and rendering dropdowns for known categorical values while leaving unknown future Palworld fields editable as generic inputs.
* **TICKET-0054** (2026-07-10, Enhancement) - Refined the World Settings grouping pass with concrete low/default/high numeric examples, compact aligned toggles, stronger alternating category separation, and a Super Admin-only Local API panel for REST/log settings.
* **TICKET-0055** (2026-07-10, Documentation) - Added a GitHub-ready `GETTING_STARTED.md` walkthrough with predictable `images/getting-started-*` screenshot placeholders and linked it from the README Quick Start section.
* **TICKET-0056** (2026-07-10, Bug) - Fixed World Settings visual alignment by removing the category heading filler line and rendering boolean settings with the same label-above-control layout as numeric/dropdown fields.
* **TICKET-0057** (2026-07-10, Documentation) - Rewrote the Getting Started guide for younger/non-technical users with simpler steps, shorter text, and more screenshot placeholders.
* **TICKET-0058** (2026-07-10, Bug) - Updated World Settings boolean controls so the setting name remains the field header and the toggle box text reads `Enable` or `Disable`.
* **TICKET-0059** (2026-07-10, Documentation) - Added the available local Getting Started screenshot files to git so GitHub can render them and recorded which guide placeholders are still missing images.
* **TICKET-0060** (2026-07-10, Release) - Rebuilt the packaged executable and installer so the installed app includes the corrected World Settings toggle layout from TICKET-0058; installer checksum is `6636805A7374892F3A9EE146CCF08C7461C3FE129BC396FB0C8F83933600B9DF`.
* **TICKET-0061** (2026-07-10, Enhancement) - Palworld config update check: diffed curated World Settings metadata against the live installed server's `DefaultPalWorldSettings.ini` (Steam build 24088465, not just the public docs page, which turned out incomplete) and found the schema grew to 119 fields with only 62 curated. Added grouped labels/help for the meaningful new gameplay settings (guild management, voice chat range, PvP damage/kill-drop config, stat-point-allocation locks, respawn penalty tuning, native join/leave message toggle, Global Palbox import/export), leaving genuinely internal/anti-cheat/client-input fields in the existing generic fallback. Fixed `bHardcore`'s now-stale help text (Pal permadeath is its own separate `bPalLost` field) and hid the new `PublicIP` ini field from the generic editor so it can't become a second, inert place to set the public IP alongside Launcher Options. No executable rebuild performed (backend/frontend source change only).
* **TICKET-0062** (2026-07-10, Feature) - Added an explicit Install / Update / Uninstall radio-button page to `installer.iss`, right after the Welcome page. Uninstall runs the real Inno-managed uninstaller (looked up via the `HKA` uninstall registry key, not the data-folder-based `ExistingSetup` check, since app data is deliberately kept after uninstall) and exits the setup process immediately, after a confirmation that explains server data/config is kept. Compiled successfully with a real `ISCC.exe`; a human still needs to click through the new page interactively before shipping, since this sandbox can't drive the interactive desktop session to verify the GUI itself (same limitation as TICKET-0018).
* **TICKET-0063** (2026-07-10, Bug) - User-reported bug right after using TICKET-0062's new Uninstall option: reinstalling afterward never asked to create an admin account, since `users.json` lived in the same app-data folder deliberately kept for real Palworld server references, and the installer's single `ExistingSetup` flag treated any surviving app-data file as "skip all first-run pages." Split it into two independent checks (`HasAdminAccount` on `users.json`, `HasServerData` on `instances.json`) and added `CurUninstallStepChanged` so uninstalling - through any path, not just the installer's own Uninstall option - clears `users.json`/`system_settings.json` while leaving `instances.json`, mods, and backups alone. Reinstalling now correctly re-asks for an admin account without redundantly re-asking to deploy a server that's already registered. Compiled clean with a real `ISCC.exe`; traced the exact reported scenario by hand but did not live-test against the user's actual current install (would have reset their real admin account outside a moment they chose).
* **TICKET-0064** (2026-07-10, Documentation) - Rewrote `NEXUS_DESCRIPTION.md`'s Installation/First-Time Setup/Troubleshooting sections to match `GETTING_STARTED.md`'s fuller numbered walkthrough (screenshots, tips, warnings, quick fixes), converted to Nexus BBCode since Nexus's description editor doesn't render Markdown. GitHub alert blocks became `[quote]` blocks with a bold label; images use `PASTE-NEXUS-IMAGE-URL: <filename>` placeholders since relative repo image paths don't work on Nexus. Kept the Nexus-only sections (features, security, requirements, limitations, support, credits, changelog) that `GETTING_STARTED.md` never covered.
* **TICKET-0065** (2026-07-10, Release) - Cut version 1.0.2: bumped `installer.iss`'s `MyAppVersion`, converted `CHANGELOG.md`'s running section into a proper `## 1.0.2` release section (TICKET-0050 through TICKET-0064), did a full rebuild (frontend + PyInstaller + Inno, not just recompiling the installer script against stale output, since TICKET-0061 changed backend Python the previous packaged exe predated), tagged `1.0.2`, and published a GitHub Release with `PalworldServerAdmin-Setup.exe` attached (SHA256 `50f9f4615efc6fa34239169fbde1f08cb8e02041df7354e4e8b0abbb34c5794b`).
* **TICKET-0066** (2026-07-10, Feature) - Added v1 multi-language support (i18n) via `react-i18next`: a TopBar dropdown (flag + native name) lets each logged-in user pick English, Chinese Simplified, Japanese, German, French, or Spanish, persisted server-side per user account (`app/services/auth.py`'s new `language` field/`set_language()`, `PATCH /api/auth/me/language`) so it follows the account across sessions/devices rather than living in browser `localStorage`. Translations for all six languages were written directly (no translation service), covering the Sidebar nav and TopBar titles/subtitles/instance-switcher/user-menu strings as the v1 surface - other pages remain English-only pending follow-up tickets that extend `web/src/i18n/locales/*.json`. Verified the real persistence path end-to-end via `curl` against the running backend (register → set language → fresh login session still shows it; invalid codes rejected with 400) and confirmed pre-existing real user accounts without a `language` key still load via the `public_view()` fallback. Frontend build/lint passed; actual browser click-through of the dropdown was left for the user, since this sandbox can't drive a real interactive browser window (same limitation as TICKET-0018/TICKET-0062's installer GUI).
* **TICKET-0067** (2026-07-10, Feature) - Extended i18n to World Settings itself: all 99 curated fields' labels, help text, descriptions, and dropdown options are now translated into all 6 languages (`web/src/i18n/locales/*.json`'s new `worldSettings` key), plus added curated English labels for 22 fields in `app/services/palworld_settings.py` that previously fell back to an auto-humanized key. Critically, only *display* text is translated - the dropdown `value` sent to/from the backend and written into `PalWorldSettings.ini` is always Palworld's exact English enum string, never translated or looked up by locale; verified this by writing `Difficulty=Hard` through the real API while the account's language was set to a non-English locale and confirming the literal English string landed in the real `.ini` file on disk. Also found and fixed a dev-loop gotcha: `uvicorn --reload` did not reliably reload after some edits to `palworld_settings.py` mid-session (stale labels kept being served) - a full process restart was needed to confirm the new labels were actually live, worth remembering for future backend-only edits verified through a long-running dev server.
* **TICKET-0068** (2026-07-10, Feature) - Translated the entire rest of the app after the user pointed out only World Settings had been done: Dashboard/player roster, Mods/Nexus/UE4SS, Server Control, Launcher Options, Logs, Settings (startup recovery, users/invites, instances, automation/backups, deploy/import wizards), and Super Admin (Local API, port forwarding, remote access, Nexus integration) - all now translated into all 6 languages using the same `t(key, {defaultValue: englishText})` pattern (no separate English locale file needed). Fixed a gap TICKET-0066 missed along the way: the server run-state word ("online"/"offline"/etc.) shown by `CrystalStatus` in both Dashboard and TopBar was still hardcoded English - added shared `serverControl.states.*` keys. Deliberately left untranslated: Palworld launch-flag names (real CLI arguments, not display text) and the Logs page's live activity-feed message text (generated server-side by `activity_log.py`, not app UI copy).
* **TICKET-0070** (2026-07-10, Enhancement) - Gave the Sidebar's super-admin-only nav items (Launcher Options, Settings, Super Admin) a distinct visual treatment - small gold crown badge on the icon, warmer gold styling, and a "Host Controls" section divider - so the super admin can visually tell which pages only they can see, without changing anything for regular admins (those items were already nav-hidden/route-guarded for them).
* **TICKET-0071** (2026-07-10, Bug) - Fixed Super Admin's Local API panel: the "REST API Enabled" toggle showed the field name inside the toggle box instead of as a title above it with "Enable"/"Disable" wording inside, unlike World Settings' boolean fields (already fixed in TICKET-0058). Made `LocalApiSettingsPanel.tsx`'s `LocalApiField` bool branch match that same layout.
* **TICKET-0069** (2026-07-10, Feature) - Implemented the Steam query port option (previously just a research ticket): added a per-instance `queryPort` in `instances.json` (default = that instance's own `gamePort`, already unique per instance, so existing single-instance hosts need zero action), passed via `-queryport=` in `process_manager.py`, editable in Super Admin's `PortForwardPanel.tsx`, and covered by the existing generic firewall/UPnP forwarding endpoints (`network.py`'s `/upnp/status` now also reports `queryPort`/`queryMapping`) whenever it differs from the game port. Verified live against the real `Tester1` instance: confirmed migration backfilled `queryPort` correctly, confirmed persistence via direct HTTP calls, and confirmed the new endpoint is super-admin-gated (403 for a regular admin).
* **TICKET-0073** (2026-07-10, Enhancement) - Two follow-ups from user testing of the 1.0.3 build: made the Sidebar's "Host Controls" divider (TICKET-0070) a solid gold badge instead of a barely-visible thin line/tiny icon that the user genuinely couldn't spot even though the crown-badged nav items themselves were rendering fine; and moved the Steam Query Port edit control (TICKET-0069) from Super Admin's port-forward panel to Launcher Options, alongside the other Palworld launch arguments it belongs with - Super Admin now shows it read-only for context.
* **TICKET-0074** (2026-07-10, Feature) - Added a "Run Diagnostics" button to Super Admin that runs the existing `support/diagnose-autopalexpress.ps1` tool from inside the app (elevating via the same `Start-Process -Verb RunAs` pattern as `firewall.add_inbound_rule`) and shows the resulting report inline, instead of requiring the separate Start Menu shortcut. Found and fixed a real bug while verifying: the diagnostics script's report file is UTF-16 LE (PowerShell 5.1's `Tee-Object`/`Out-File` default), not UTF-8 despite the `.txt` extension - the initial `read_text(encoding="utf-8")` silently produced corrupted text, caught via a substring check that unexpectedly returned `False` against a real generated report.
* **TICKET-0075** (2026-07-11, Bug) - Fixed Steam Query Port collisions causing Palworld to start on the next open game port. User live testing showed that when `-queryport` equals `-port`, Steam query can take that UDP port first and Palworld moves the actual game listener (example: backend logged 8213, Palworld console reported 8214). Query ports now default/migrate to a distinct nearby port, backend saves reject values matching any registered server game port or another query port, `process_manager.py` logs both game/query ports, Launcher Options warns and blocks same-port saves, and Super Admin's firewall/UPnP checklist covers the separate query port.
* **TICKET-0076** (2026-07-11, Bug) - Fixed the Super Admin "Run Diagnostics" button failing with only the "you may have declined the permission prompt" toast. `diagnostics.run()` now attempts the elevated PowerShell helper first, but if UAC/elevation fails it runs the same script without admin rights and returns the limited report inline with a clear warning that firewall inspection may be incomplete. Verified by forcing the fallback path in Python and confirming a report path/text is returned; frontend and backend builds passed.
* **TICKET-0077** (2026-07-11, Release) - Rebuilt the frontend, PyInstaller executable, and Inno installer after TICKET-0075/TICKET-0076. The first Inno run hit Windows "Access is denied" from the sandbox, then succeeded when rerun with approval. Current `installer_output/PalworldServerAdmin-Setup.exe` SHA256 is `D07A9649E10605DFB8CDD301AB07778B88EEAE7AB2D7E9039373C1F4F620C91C`.
* **TICKET-0078** (2026-07-11, Feature) - Added a default-off Steam Query Port toggle in Launcher Options. Existing/new instances migrate with `useQueryPort=false`; the saved query-port value remains available but `process_manager.py` only appends `-queryport=<port>` when the toggle is enabled. Launcher Options disables/grays the query-port input while off, and Super Admin's firewall/UPnP query-port readout/steps only appear when enabled. Backend compile and frontend build passed.
* **TICKET-0079** (2026-07-11, Release) - Rebuilt the frontend, PyInstaller executable, and Inno installer after TICKET-0078. The first Inno run hit Windows "Access is denied" from the sandbox, then succeeded when rerun with approval. Current `installer_output/PalworldServerAdmin-Setup.exe` SHA256 is `57404DC5DAE6FA3BEDF9E5B5D8584AC1AAA94B01DFDF1A8506A44F0E1DAE7322`.
* **TICKET-0080** (2026-07-11, Release) - Cut version 1.0.4. Discovered `CHANGELOG.md`'s `## 1.0.3` section had drifted from what the real `v1.0.3` tag actually shipped, since TICKET-0073 through TICKET-0078 kept getting appended to it after that release already went out; restored it to match `git show v1.0.3:CHANGELOG.md` (TICKET-0066/0067/0068/0069/0070/0071 only) and created a proper `## 1.0.4` section for TICKET-0073/0074/0075/0076/0078, leading with a callout for TICKET-0075's Steam Query Port/game-port collision fix per the user's request. Bumped `installer.iss` to 1.0.4, full rebuild (frontend + PyInstaller + Inno). New installer SHA256 is `5B726B97261CBB18DBA81A4E4AE5261AFDCF4E9A6386F341B9B043833E429CE6`.
* **TICKET-0081** (2026-07-11, Bug) - Fixed three Logs page problems: real client IP addresses leaking into the "AutoPalExpress" panel for any logged-in user (traced to uvicorn's own access log, not `activity_log.jsonl` - now masked for non-super-admins via a new `mask_ips` param on `app_log_reader.read_tail()`), recurring low-value polling noise mistaken for "pings" (actually `/api/server/status`/`/api/logs/streams` access-log lines, now silenced via a `logging.Filter` on `uvicorn.access` in `app/main.py` without affecting the requests themselves), and both panels appending new entries at the bottom instead of the top (the AutoPalExpress panel was natural/oldest-first order; now newest-first like the activity feed already was, with export still writing chronological order to the downloaded file).
* **TICKET-0082** (2026-07-11, Bug) - Fixed a user-reported mod install bug: an archive bundling its full relative game path (`Pal/Binaries/Win64/Mods/<ModName>/...`) installed as a doubled, non-functional `Mods/Pal/Binaries/Win64/Mods/<ModName>/...` path, since `mod_installer.extract_and_install()` only ever unwrapped one lone top-level folder and mistook `Pal` for the mod's name. Added `_detect_game_path_prefix()` to strip that exact known 4-level wrapper (bails out untouched if any level doesn't match) before the existing unwrap rule runs, applied to both `extract_and_install()` and `peek_archive_name()`. Verified against the reported shape plus a normal and a flat archive to confirm no regression.
* **TICKET-0083** (2026-07-11, Feature, OPEN - not implemented) - Filed a request to let the super admin pick which file to install when a Nexus mod has more than one file (e.g. Main + Optional Files). Researched first: `nexus_client.get_mod_files()` already fetches the full file list; `_select_installable_nexus_file()` in `app/routes/mods.py` always auto-picks the newest "Main" file and Optional Files are currently invisible with no picker UI at all. Left open with implementation notes in the ticket, not built yet.
* **TICKET-0084** (2026-07-11, Bug) - Follow-up to TICKET-0082 found by the user's live test: a second archive shape with the same bug, `Pal/Binaries/Win64/ue4ss/Mods/<ModName>/...` (an extra `ue4ss` segment some mod authors include), which TICKET-0082's stricter 4-segment-only match correctly declined to touch and so still mis-installed. Generalized `_detect_game_path_prefix()` to allow an optional `ue4ss` segment between `Win64` and the required `Mods` segment. Verified both nested shapes plus the existing regression cases.
* **TICKET-0083** (2026-07-11, Feature) - Implemented the multi-file Nexus mod picker after the user hit a real mod with 2 files and no way to choose. Split the old silent-auto-pick `_select_installable_nexus_file()` into a full candidate-list function plus a selector that defaults to the same auto-pick or honors an explicit `file_id`. New `GET /api/mods/from-nexus/{id}/files` endpoint, `install_from_nexus`/`update_mod` accept an optional `file_id`, new `NexusFilePickerDialog.tsx` shown only when a mod actually has more than one current file (single-file mods still install in one click as before). Verified the selection/listing logic directly against a synthetic Main+Optional payload.
* **TICKET-0083** (2026-07-11, Feature) - Implemented the multi-file Nexus mod picker after the user hit a real mod with 2 files and no way to choose. Split the old silent-auto-pick `_select_installable_nexus_file()` into a full candidate-list function plus a selector that defaults to the same auto-pick or honors an explicit `file_id`. New `GET /api/mods/from-nexus/{id}/files` endpoint, `install_from_nexus`/`update_mod` accept an optional `file_id`, new `NexusFilePickerDialog.tsx` shown only when a mod actually has more than one current file (single-file mods still install in one click as before). Verified the selection/listing logic directly against a synthetic Main+Optional payload.
* **TICKET-0087** (2026-07-11, Bug) - Removed the bundled `Diagnose-AutoPalExpress.cmd` after the user reported Nexus Mods' scanner flags it. The Start Menu "Diagnose AutoPalExpress" shortcut now launches PowerShell directly against `diagnose-autopalexpress.ps1` instead - no `.cmd`/`.bat` shipped at all, same shortcut name/behavior, safe since TICKET-0076 already made the script itself degrade gracefully when not elevated.
* **TICKET-0086** (2026-07-11, Release) - Cut version 1.0.5 covering TICKET-0081/0082/0083/0084, leading with the mod-unpacking fix and the multi-file picker per the user's explicit request; TICKET-0087's `.cmd` removal was found and folded in before publishing. Final installer SHA256 `11AB0B83B8230B00A3F4C8B51451CB4DA68C0756867BDC99170F540685236FD9`.
* **TICKET-0088** (2026-07-12, Bug) - Fixed Nexus Support's two review findings: `Application-Version` and the installer now both declare 1.0.6, and the installed-mod update route now requires the super admin like every other route that can use the saved Nexus key or initiate a download.
* **TICKET-0089** (2026-07-12, Feature) - Added a per-server mod wishlist. Regular admins submit public Nexus metadata only; Super Admin's new Mod Wishlist tab can approve (perform the protected keyed install) or deny each request. Duplicate requests are suppressed.
* **TICKET-0090** (2026-07-12, Feature) - Added a subtle project-styled PayPal donation form to the bottom of the sidebar. It preserves the supplied merchant/item/recurring/NOK fields, replaces PayPal's stock image with a muted heart control, and omits the tracking pixel.
* **TICKET-0091** (2026-07-12, Feature) - Added cached, credential-free GitHub release checks and a restrained sidebar update indicator. A shared runtime version feeds Nexus/UI status, packaging rejects installer-version drift, and failures remain non-fatal; the app never downloads or executes updates automatically.
* **TICKET-0092** (2026-07-12, Bug) - Fixed GitHub #154: on some Windows 10 systems the packaged frontend JavaScript was served as `text/plain`, so browsers rendered the background but blocked the login module. Explicit `.js`/`.mjs` MIME registration before static mounts now guarantees `application/javascript`; verified against an actual built asset response.
* **TICKET-0093** (2026-07-12, Bug) - Fixed a user-reported first-run bug: the installer's seeded first-server deploy can still be running (SteamCMD download) when `desktop_app.py` opens the browser, since it only waits for the port to listen, not for setup to finish. The top bar's `InstanceSwitcher` fetched `/api/instances` once on mount with no refetch, so it stayed stuck on "no servers" even after the seeded deploy actually succeeded, and the user's same-name retry correctly hit the "install folder already exists" check. Now polls every 3s until an instance appears, then stops.
* **TICKET-0094** (2026-07-12, Enhancement) - User-annotated screenshot request: moved Mod Wishlist approval (TICKET-0089) off a second Super Admin tab and onto its own sidebar page. Added a super-admin-gated `/mod-wishlist` route rendering the existing `ModWishlistPanel`, added a "Mod Wishlist" entry to the Sidebar's `HOST_ITEMS` (first, directly under the Host Controls divider), reverted `SuperAdmin.tsx` to its plain single-page layout, and added the missing `nav`/`topbar.pages` translation keys across all 6 locales (the wishlist feature had only ever shipped with English `defaultValue` fallbacks, never real translations).
* **TICKET-0095** (2026-07-12, Enhancement) - Added `scripts/build.bat`, a thin one-command wrapper around the existing `build_installer.ps1` (frontend + PyInstaller + Inno Setup), satisfying both the user's direct request for a batch file and this project's standing build-script requirement. Used it to produce an installer covering TICKET-0092/0093/0094 (SHA256 `4239c57b31b9ae30a8ca8ef30023f6e05218330fb5acddc2da35d45bdb7d599d`).
* **TICKET-0096** (2026-07-12, Documentation) - Refreshed the README to catch up with TICKET-0088 through TICKET-0095: added the sidebar donation link to the feature list and documented `scripts\build.bat` as the recommended installer build command. Validated first by fetching `origin` and diffing `README.md` against `origin/main` - no divergence, so no online-only edit existed to reconcile.
* **TICKET-0097** (2026-07-12, Release) - Cut version 1.0.6 covering TICKET-0088 through TICKET-0096. `installer.iss`/`app/version.py` already declared 1.0.6 from TICKET-0088, so no version bump was needed. Converted `CHANGELOG.md`'s `Unreleased` section into a proper `## 1.0.6` release section, rebuilt via `scripts\build.bat`, updated the checksum in `CHANGELOG.md`/`README.md`/`installer_output/CHECKSUMS.txt`, tagged `v1.0.6`, and published a GitHub Release with the installer attached, with notes highlighting the mod wishlist's Nexus Mods API-key compliance angle per the user's request. Final installer SHA256 `aabaddd84a80676ea753e925d85fea23e4bd5a1ba80444edbd738149e3139b83`.
* **TICKET-0098** (2026-07-12, Documentation) - Added a `wiki/` folder with one article per sidebar page (9 total, matching `Sidebar.tsx`'s real nav items and labels), each written from the actual page components rather than guessed, plus a `wiki/README.md` index. Each article has a screenshot placeholder pointing at `images/wiki/<page>.png`; created that folder (via `.gitkeep`) for the user to drop real screenshots into later. Pre-approved by the user in advance since they were stepping away.
* **TICKET-0099** (2026-07-12, Bug / Enhancement) - Two follow-ups after the user said they saw nothing on GitHub: (1) `git push` had started failing with a real 403 (stale Git Credential Manager login, unrelated to `gh` CLI's still-valid auth) - fixed by `gh auth setup-git`, which pointed git at `gh`'s working credentials, then pushed TICKET-0098's stranded commit. (2) TICKET-0098's `wiki/` folder lives in the main repo's file tree, not GitHub's actual Wiki tab (a separate `AutoPalExpress.wiki.git` repo) - found the repo already had Wikis enabled with a pre-existing Home/Getting-Started page made directly on GitHub, read both before touching anything, then added the same 9 sidebar pages there too (wiki-local `images/<name>.png` paths, matching the existing page's own image convention) with a Home.md nav index. Kept the in-repo `wiki/` folder as a versioned copy alongside the live wiki, consistent with this project's existing GETTING_STARTED.md/wiki-Getting-Started precedent of parallel per-venue docs.
* **TICKET-0100** (2026-07-12, Documentation) - User feedback: the wiki pages were too generic. Rewrote all 9 pages (both the in-repo `wiki/` copy and the live GitHub Wiki) as numbered "How do I...?" walkthroughs instead of feature-list bullets, with a separate screenshot placeholder per step, each naming exactly what to circle/highlight, using a numbered filename convention (`<page>-01-...png` etc.) matching the project's existing Getting Started docs. Pushed both.
* **TICKET-0101** (2026-07-12, Documentation) - User captured all 55 screenshots from the TICKET-0100 checklist into `Pictures`; moved them into `images/wiki/` in the main repo and into `AutoPalExpress.wiki.git`'s own `images/` folder, fixing one typo'd filename (`mods-03-install-or-wishlis.png` -> `...wishlist.png`) on the way in. Removed the now-redundant `images/wiki/.gitkeep`. Pushed both.
* **TICKET-0102** (2026-07-12, Documentation) - Stripped the leftover `*(Screenshot placeholder ...)*` caption lines from all 9 pages, in both the in-repo `wiki/` copy and the live GitHub Wiki, now that TICKET-0101 replaced every placeholder with a real image. Pushed both.
* **TICKET-0103** (2026-07-12, Documentation) - Referenced the new `images/wiki/` screenshots from `README.md` (added a Server Control/World Settings row to the Screenshots gallery, plus a Wiki link) and `GETTING_STARTED.md` (Wiki link near the top; illustrated the "Add to Wishlist" button in the Add Mods step; fixed the long-missing diagnostics screenshot by swapping in `super-admin-06-diagnostics.png` and mentioning the in-app Run Diagnostics button alongside the Start Menu shortcut).
* **TICKET-0072** (2026-07-13, Feature) - Long-open ticket, finally implemented: new `web/src/components/ui/password-input.tsx` (`Eye`/`EyeOff` reveal toggle around the base `Input`), swapped into login/register, first-run setup account+confirm, and every World Settings/Local API `sensitive` field. Masked by default. Aria-label translation into the 5 non-English locales deliberately skipped - confirmed `components/ui/*` primitives elsewhere in this codebase have no i18n of their own either, so this isn't a new gap.
* **TICKET-0110** (2026-07-13, Enhancement) - Moved "Mod File Uploads" (Install From File) off Super Admin onto the Mod Wishlist page, above the request list - both are super-admin mod-installation actions and now live together. `ModWishlist.tsx` grew from a thin wrapper into hosting both; `SuperAdmin.tsx` lost the section and its now-unused imports/state.
* **TICKET-0114** (2026-07-13, Enhancement) - Moved Import Save from Settings > Automation to Settings > Server Instances' header (alongside Import Existing/Deploy New Server), styled `variant="arcane"` (purple) per user request. `onImported` is a no-op in its new home since nothing on that panel needs refreshing after a save import.
* **TICKET-0111** (2026-07-13, Enhancement) - Two fixes: (1) `RemoteAccessPanel`/`PortForwardPanel` no longer pop their whole card/body in once network status resolves - new shared `web/src/components/fantasy/Skeleton.tsx` stands in for just the data-dependent parts while real section labels render immediately. Along the way fixed a related bug: `firewallOk === null` (still loading) was previously indistinguishable from "confirmed not allowed," showing a misleading "Not allowed yet" before the check had even run. (2) Every boolean toggle in World Settings/Local API Settings now reads a static "Enable or Disable" instead of flipping between the words based on current state (new `worldSettings.chrome.enableOrDisable` key).
* **TICKET-0113** (2026-07-13, Bug) - User report: Dashboard's "Last saved" never updated despite clicking Save World repeatedly. Root cause: `server_control.py` hardcoded `lastSavedAt: ""` and never actually stored the timestamp a save produced anywhere `/status` could read back - it was returned once in the `/save` response and then thrown away. Fixed with a new in-memory `process_manager._last_saved_at` store (`record_save()`/`get_last_saved()`, same pattern as `_started_at`), updated by both manual Save World and `backup_service.run_backup()`'s live-save step.
* **TICKET-0115** (2026-07-13, Feature) - Dashboard CPU/RAM tiles were already Palworld-process-only with no whole-machine metric anywhere in the API. Added `_system_load()` in `server_control.py` (`psutil.cpu_percent(interval=None)` + `psutil.virtual_memory().used`) merged into `_status_view()`; both tiles now render two `ManaProgressBar`s ("Palworld" / "System") using that component's existing `label`/`valueLabel` props - no `StatTile` API change needed.
* **TICKET-0112** (2026-07-13, Feature) - New Dashboard traffic lights (`TrafficLight.tsx`, `NetworkStatusLights.tsx`) for Game Port/Steam Query Port/Remote Access, summarizing the same firewall+forwarding checks `PortForwardPanel`/`RemoteAccessPanel` already do. Found while implementing that `/api/network/*` is already `require_super_admin`-gated at the router level (not "visible to every admin" as the ticket had assumed) - rendered only for `user.role === "super_admin"` instead of loosening that boundary.
* **TICKET-0116** (2026-07-13, Feature) - New Super Admin "Run Silently" toggle (off by default) hiding both AutoPalExpress's own console window and Palworld's server console window from the next start onward. Palworld's side reuses the exact `CREATE_NO_WINDOW` mechanism TICKET-0019/TICKET-0023 already proved out; AutoPalExpress's own side uses `GetConsoleWindow()`/`ShowWindow()` in a new `desktop_app._apply_run_silently()`. `process_manager.py` reads the setting straight from `app.storage` rather than importing `system_settings` (which already imports `process_manager` - the reverse would be circular). **REVERTED in TICKET-0120** after three follow-on bug tickets never reached a stable state.
* Batch-rebuilt and shipped together (checksum `D6A3132784C7B5DC3F37E5C4A4DB8F655CE52C206CD7CE6AF7464C4B6831FFDA`) - all 8 tickets above were implemented in one sitting per explicit user pre-approval ("make all tickets, i preapprove everything").

* **TICKET-0120** (2026-07-13, Bug) - User asked to fully revert Run Silently (TICKET-0116/0117/0118/0119) rather than continue debugging it: "revert the option to run silent, it messes up things, roll back." Used `git revert --no-commit` on the three isolated commits (TICKET-0119/0118/0117), then manually stripped the remaining TICKET-0116-originated code that predated those (system_settings.py's `runSilently` field, process_manager.py's conditional `CREATE_NO_WINDOW`, the deleted `RunSilentlyPanel.tsx` and its `SuperAdmin.tsx` wiring, `models.ts`'s field, README/CHANGELOG mentions). Caught a real mistake mid-revert worth remembering: reverting the three commits newest-first actually restored `desktop_app.py` to TICKET-0116's *original* broken `ShowWindow`-based version, not a clean pre-0116 state - because TICKET-0119 was the commit that had fully removed that code, so reverting TICKET-0119 undid the removal rather than the addition. Caught by a full repo-wide grep for leftover references after the "clean" revert, not assumed. `tickets/closed/TICKET-0116.md` kept (not deleted) with a "REVERTED" note and a retrospective, since a git revert already erased TICKET-0117/0118/0119's own ticket files as a side effect of reverting those commits. Rebuilt and shipped a single-exe installer again (checksum `8AAD9B9F2C2F26C0C09647A22A9D071CE041C9296178E21C9DB3F934AF527803`).
* Rebuilt again with no source changes for user's plain "make new installer" request; confirmed non-deterministic build metadata alone changes the checksum even with an identical frontend bundle hash. New checksum `20310940C489D1B46B020538843A4B0F0A43BA60F5698451FFD63A3589239578`, published to README/CHECKSUMS.txt (installer_output/ is gitignored, so only README.md needed a commit). Also cleaned up a stray leftover `dist/PalworldServerAdminSilent.exe` from the pre-revert (TICKET-0119-era) build that was confusing "is Run Silently really gone?" checks - it wasn't part of the current single-EXE spec at all, just an unrelated file PyInstaller never deletes on its own.
* **TICKET-0121** (2026-07-13, Enhancement) - User report: Launcher Options ("Host Control") was also blocking behind a full-page spinner while `instancesApi.getActive()`/`networkApi.getUpnpStatus()` were in flight - same complaint as TICKET-0111, different page that ticket didn't cover. Applied the identical fix: `LauncherFlags.tsx` now renders the `ScrollPanel` shell with skeleton-filled cards (matching the real grid's 4 compact + 3 wide layout) while `!instance`/`!loaded`, instead of a centered "Loading launcher options..." message; the "select a server" empty state now only shows once loading has actually finished with no instance found.
* **TICKET-0122** (2026-07-13, Enhancement) - User report: a mod installed via Super Admin's Install From File was showing "Request Update," which shouldn't apply since it wasn't installed through the Nexus download pipeline. Root cause: `confirm_install_from_file()` sets a real `sourceModId` (from the Nexus hash-verification lookup that gates manual uploads), so `_with_update_status()` treated it identically to a real Nexus install for update-checking purposes. Fixed without any new persisted field or migration: both mod-creation paths already use a distinguishable id prefix (`mods_store.new_id("nexus")` vs `new_id("verified")`), so `manuallyInstalled` is now derived from the `verified-` prefix at response time, manual mods are excluded from the update-check's `mod_ids` list entirely, and `ModCard.tsx` shows a "Manually Added" badge whenever the flag is true.
* **TICKET-0123** (2026-07-13, Feature) - User request: "nothing goes into appdata folder. Everything should live inside the install folder." Confirmed two scope decisions via AskUserQuestion first (per this project's "never assume architecture" rule): a single user-picked install location with no admin-elevated path (removed `PrivilegesRequiredOverridesAllowed=dialog` from `installer.iss`, keeping `PrivilegesRequired=lowest` so the chosen folder is always writable), and auto-migration of existing users' data with a one-time notice. `app/paths.py`'s `data_dir()` now resolves to `install_dir()/"data"` when frozen; new `migrate_legacy_data_if_needed()` does a one-time `shutil.move()` of any real `%LOCALAPPDATA%\PalworldServerAdmin\data` into that new location, called first thing in `desktop_app.main()` with a one-time `MessageBoxW` notice on success (generalized the old error-only `_show_startup_error` into `_show_message_box(message, icon)` to support an information icon too). `installer.iss`'s whole `[Code]` section (first-run seed paths, startup-recovery settings, the Diagnose shortcut's `-DataDir`/`-ReportDir`, `CurUninstallStepChanged`) moved from `{localappdata}\PalworldServerAdmin\data` to `{app}\data`; `HasAdminAccount`/`HasServerData` check both locations since the wizard's page-skip logic runs before the Python app's own migration ever executes. Verified thoroughly: unit-tested the migration function's three paths directly (real migration, already-migrated no-op, fresh-install no-op) via `sys.frozen`/`LOCALAPPDATA` simulation, did a full `scripts\build.bat` rebuild (Inno Setup's rewritten `[Code]` section compiled clean), then ran the *real built exe* twice against isolated scratch folders with `$env:LOCALAPPDATA` overridden - once seeded with fake legacy data (confirmed it moved into the scratch install folder and the legacy folder vanished) and once with none (confirmed a completely clean boot to `HTTP 200` with everything self-contained under the scratch install folder). New installer checksum `5466E7F90BEBFA8A843DA31144BCE770F1604DD5EB4BF47DE57C93BE9223F919`.
* **TICKET-0124** (2026-07-13, Enhancement) - User request made mid-conversation while TICKET-0123 was in progress: add a "Run Diagnostics as Admin" button alongside the existing "Run Diagnostics" (which already tries elevation first and silently falls back to a limited report per TICKET-0076). `diagnostics.run()` gained a `force_admin` parameter that raises `DiagnosticsError` instead of silently falling back when elevation fails; `/api/system-settings/diagnostics` now accepts an optional `{ forceAdmin }` body (defaulted so old callers/empty bodies still work); `DiagnosticsPanel.tsx` got a second `ShieldCheck`-icon ghost-variant button. Bundled into the same TICKET-0123 rebuild.
* **TICKET-0125** (2026-07-13, Bug) - Regression reported live immediately after the TICKET-0123 build: "internal error: an attempt was made to expand the app constant before it was initialized." Root cause: `HasAdminAccount`/`HasServerData`/`ServerInstallDirPage`'s default value all switched to expanding `{app}` in TICKET-0123, but were being evaluated from `InitializeWizard`, which runs before Setup has even shown the destination-folder page - `{app}` has no value to expand to that early (unlike `{localappdata}`, which never had this dependency). Fixed by moving all three to fire from `NextButtonClick(CurPageID = wpSelectDir)` instead, the earliest point `{app}` is guaranteed valid. Verified past a simple "process didn't immediately exit" check (which can't distinguish a live error dialog from a live wizard) by using `EnumWindows`/`GetWindowText` against the real running installer to confirm its actual visible window title was the normal wizard, not an error box. New checksum `036F86C17A537B2792E05217239C52AF7CA177232BBC9A2B607D161B1FD03F94`.
* **TICKET-0128** (2026-07-13, Bug) - User reported a second installer problem right after TICKET-0125: "access is denied" on a specific file partway through install. No existing registered install was found (checked the real uninstall registry keys directly), ruling out an upgrade-path cause. A real `/VERYSILENT` install with an explicit writable `/DIR=` succeeded end-to-end, proving the core mechanism was fine - the actual bug is that since TICKET-0123 made Setup never elevate, a folder that genuinely needs admin rights (most plausibly something under Program Files, whether typed by hand or defaulted to on some environment) fails deep in the file-copy step with a bare, unexplained OS error. Added `CanWriteToDir()` - a real temp-file write test against `{app}` - to the existing TICKET-0125 `NextButtonClick(wpSelectDir)` handler; an unwritable folder now shows a clear explanation immediately and keeps the user on the directory page instead of proceeding toward a guaranteed later failure. Verified both outcomes with real `/VERYSILENT` installs (confirmed via the Inno log's own "Message box (OK):" transcript for the blocked case, and a clean "Installation process succeeded" for a writable scratch folder). Two process lessons worth remembering, documented in the ticket: (1) even a scratch-`/DIR=` silent test install still writes to the machine's *real* Start Menu/Desktop/HKCU registry (not redirected by `/DIR`) - two real accidental installs during this investigation were found and cleaned up immediately rather than left dangling; (2) `Start-Process -ArgumentList` with a plain array doesn't auto-quote elements containing spaces, which silently truncated `/DIR="C:\Program Files\..."` down to `C:\Program` on the first attempt. New checksum `73A3418EB3E43295DCEE7F4D32F67636787453B753B69752BAC048B375FF4712`.
* **TICKET-0127** (2026-07-13, Enhancement) - User request: "make sure the program installs as AutoPalExpress, not Palworld Server Admin," confirmed via AskUserQuestion to also rename the actual installed .exe (not just installer display text). Renamed `PalworldServerAdmin.spec` -> `AutoPalExpress.spec` (`name='AutoPalExpress'`), `installer.iss`'s `MyAppName`/`MyAppPublisher`/`MyAppExeName`/`OutputBaseFilename` (AppId GUID deliberately unchanged - that's what Inno actually tracks upgrades by), added an `[InstallDelete]` entry for the stale old-named exe on upgrade, renamed `system_settings.py`'s startup-recovery exe check, `network.py`'s `ADMIN_FIREWALL_RULE_NAME` (with a `_LEGACY_ADMIN_FIREWALL_RULE_NAME` fallback in `firewall_status` so an existing pre-rename firewall rule still counts as allowed), `desktop_app.py`'s MessageBox title/text, and `RemoteAccessPanel.tsx`'s suggested router-rule label. Deliberately left `app/paths.py`'s legacy AppData folder-name constant's *value* unchanged (renamed to `_LEGACY_APP_DIR_NAME` for clarity only) since TICKET-0123's migration lookup depends on it matching old installs' real on-disk folder name verbatim. Verified thoroughly against the real rebuilt artifacts, not just source: `EnumWindows` confirmed the wizard's real title is now "Setup - AutoPalExpress version 1.0.7"; a real `/VERYSILENT` install confirmed every shortcut and the Start Menu folder itself read "AutoPalExpress"; a real boot test of the renamed `AutoPalExpress.exe` succeeded (`HTTP 200`). New checksum `8B8794ACC0F597C8B0786D23A08AD7F5FDC40E5010D61D463B567AFAB74D01A4`. Known accepted limitation: an upgrading (not fresh) install keeps its old Start Menu *folder* name since Inno remembers that by AppId independent of `DefaultGroupName` - only the shortcut inside it renames.
* **TICKET-0129** (2026-07-13, Enhancement) - Immediately after shipping TICKET-0123 ("everything self-contained in the install folder, no admin installs"), user pushed back on both halves of that decision: hit the new write-guard (TICKET-0128) trying to install to Program Files and said "i want it to install to program files like any other normal program," then when offered a choice between restoring elevation-with-shared-permissions vs. the standard Windows split, picked a third option: "could we use my documents instead of appdata? i dont like how appdata is so hidden." Landed on the actual standard convention: Program Files (or wherever, elevated or not) holds only the program (`installer.iss`'s `PrivilegesRequiredOverridesAllowed=dialog` restored), while ALL data (accounts, server registry, mods, logs, backups) now lives under `Documents\AutoPalExpress\data` via a new `app/paths.py:_documents_dir()` (`SHGetFolderPathW`/CSIDL_PERSONAL, honors OneDrive-style redirection) - completely independent of wherever the program itself ends up. `migrate_legacy_data_if_needed()` now checks two legacy locations in order (TICKET-0123's in-install-folder data, then the original pre-0123 AppData location) and moves whichever is found into the new Documents home; `installer.iss`'s `HasAdminAccount`/`HasServerData` check all three locations for the same reason. Verified directly: all three Python migration paths tested via `sys.frozen`/mocked `_documents_dir()` simulation; a real rebuilt `AutoPalExpress.exe` run standalone confirmed data lands at the real `Documents\AutoPalExpress\data` (cleaned up immediately after); a live elevated Program-Files install specifically was NOT attempted, since this environment's own safety guard correctly blocked a `Remove-Item` under `C:\Program Files`, leaving no safe way to clean up such a test here - covered instead by reviewing the restored Inno Setup config against this project's own pre-TICKET-0123 history, where it worked correctly for months. This is the second decision reversal-under-pressure this session (see TICKET-0116->0120 for the first) - the actual final shape is recorded carefully in `decisions.md`. New checksum `D77136F8AC121AFEA3FB967C00A317970A195D355992B1B787B9AF16FE1B962B`.
* **TICKET-0130** (2026-07-13, Enhancement) - User request right after TICKET-0129 shipped: "some users will be updating from a previous install. please run a detection where you search for the old files and give the users the options to migrate to the my documents folder or to start a brand new server." Split the old silent-automatic `migrate_legacy_data_if_needed()` into pure `detect_legacy_data_dir()` (checks both legacy locations, touches nothing) and `migrate_data_dir()` (only called after the user agrees), plus a non-creating `documents_data_dir()` used to detect "has this already been decided" without side effects. `desktop_app.py`'s new `_offer_legacy_data_migration()` shows a real Yes/No native dialog when legacy data is found; "No" leaves the old data completely untouched and creates a fresh empty Documents folder immediately so the question isn't repeated next launch. Verified thoroughly at the unit level (both the migrate and start-fresh paths, plus the "don't ask twice" behavior); a live interactive click-through via `SendKeys` automation against the real compiled exe was attempted but abandoned given this sandboxed environment's known limits driving interactive native dialogs (consistent with earlier-noted GUI-automation limits, e.g. TICKET-0018/0062) - test artifacts from the attempt were found and cleaned up immediately. New checksum `3B554788C45374439415F6B9B3B080C70E8030180C71A2C2C68721211D10C806`.
* **TICKET-0131** (2026-07-14, Bug, critical) - User report, mid-conversation with TICKET-0129/0130: clicking Browse on the Import Server dialog closed the entire AutoPalExpress window/process outright. Root cause: `native_dialog.pick_folder()` created a `tkinter.Tk()` root, but every one of its four callers (`instances.py` x2, `mods.py`, `automation.py`) invokes it via `asyncio.to_thread` - meaning `Tk()` initializes on a worker thread, never the main thread uvicorn's event loop already owns. Tcl/Tk isn't safe to initialize outside the main thread; doing so was crashing the whole packaged process with a low-level native failure that bypasses normal Python exception handling, matching "the whole window closed" rather than a graceful error. Fixed by replacing the tkinter implementation with a short-lived PowerShell subprocess showing `System.Windows.Forms.FolderBrowserDialog` (same signature, zero call-site changes) - a subprocess has its own real main thread, sidestepping the problem entirely, and matches this project's existing pattern of shelling out to PowerShell for native Windows integration (`firewall.py`, `diagnostics.py`). Verified with two real, live end-to-end tests (not just code review): a Cancel path (dialog found via `EnumWindows`, closed via `WM_CLOSE`, returned `None`) and a real-selection path (clicked the dialog's actual OK button via `GetDlgItem`/`BM_CLICK`, correctly returned the chosen path) - both driven through the exact `asyncio.to_thread` call pattern the real routes use, and neither crashed. New checksum `BB64DE66133DF658467C856419CDF9DC081C36E6D228E7A6E611983E8D6CFE1D`.

---

Continue adding completed tickets in chronological order.
