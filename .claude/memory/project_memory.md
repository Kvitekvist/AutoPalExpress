# Project Memory

This file represents the long-term memory of the project.

Update continuously.

---

## Project Vision

A self-hosted Palworld Dedicated Server admin panel that lets a small group of friends jointly run a modded server without anyone needing SSH access, shared admin passwords, or manual config-file editing. The host is the permanent "super admin"; friends register via invite code as regular admins with full day-to-day operational access. Optimized for "a handful of friends running their own server," not a hosted multi-tenant SaaS.

---

## Current Milestone

Core feature set is complete and has been exercised live: multi-instance server management (deploy new via SteamCMD, or import existing), real process control, a generic World Settings (`.ini`) editor, Nexus GraphQL mod browsing that's wishlist-only for every role as of TICKET-0153 (the super admin still installs via Mod Wishlist approval or Install From File, both using the saved Premium API key/hash verification, just no longer a one-click button in the browse dialog itself), UE4SS installer, networking (UPnP port forwarding, Windows Firewall automation, public IP display), multi-user auth with invites, and Palworld REST API-backed scheduled backups/restarts, restart warnings, join/leave announcements, metrics, and Players page actions (list/kick/ban/unban). A real automated backend test suite and GitHub Actions CI landed in TICKET-0154 - see `architecture.md`'s Testing & CI section. Backups and Save Import both got a real hardening/feature pass in TICKET-0148/TICKET-0155: a shared `app/services/safe_replace.py` primitive (copy-to-temp/verify/atomic-swap) backs both Save Import's deeper world-save discovery + validated import and a full one-click Restore/Verify/Export/notes workflow for backups, with configurable retention replacing the old fixed 10-backup limit. Currently in a polish/hardening phase rather than adding wholesale new feature areas.

---

## Active Priorities

* COMPLETED 2026-07-16 (TICKET-0157/0158/0159): Added APE University with persisted per-user progress, backend-enforced sequential lessons, role-aware course availability, an always-visible quest tracker, diplomas, and graduation confetti. A new super admin automatically starts the Super Admin Degree; regular admins automatically start Admin Basics. Mod Supervisor unlocks only after the Super Admin diploma. The Admin Basics fake-player kick uses an isolated Captain Lamball simulator and cannot affect a real player. Verified with four dedicated API tests, the full backend suite, frontend lint, and production build.

* REST API has replaced RCON for game-level control; manual Stop/Restart now try Palworld's REST shutdown path before local process cleanup.
* `process_manager` now discovers Palworld server processes by executable name and selected server folder for status metrics, even if the backend forgot the original launcher process.
* Remaining mocked surfaces: the general Settings blob's own fields, plus per-player whisper/teleport on the Players page because Palworld's REST API has no matching commands.
* NEEDS MANUAL VERIFICATION (TICKET-0018): the new installer-driven first-run setup (custom wizard pages + progress page in `installer.iss`) compiles cleanly and every backend code path it depends on is tested via real HTTP requests, but the actual interactive GUI click-through hasn't been run through by a human yet - this dev sandbox has no interactive desktop session to drive an installer wizard with (confirmed: even a `/VERYSILENT` install hangs on an unrelated pre-existing privilege dialog). Next real install should be watched end-to-end.
* NEEDS MANUAL VERIFICATION (TICKET-0062): the new Setup Mode (Install/Update/Uninstall) page in `installer.iss` compiles cleanly with a real `ISCC.exe` and the Pascal logic was checked against Inno's documented API, but a human needs to click through all three radio choices - especially Uninstall - before this ships. Confirmed again (same as TICKET-0018) that this sandbox cannot observe or drive the interactive desktop session: window enumeration found and "focused" the real Setup window by handle, but a screen capture taken immediately after never reflected it.
* FIXED 2026-07-10 (TICKET-0063): Real-world use of TICKET-0062 immediately surfaced a bug - uninstall then reinstall never re-asked for an admin account, because `users.json` lived in the same app-data folder deliberately kept for real Palworld server references, and the installer's one combined `ExistingSetup` flag treated any surviving app-data file as "this is just an update." Replaced it with two independent checks (`HasAdminAccount` on `users.json` alone, `HasServerData` on `instances.json` alone) and added `CurUninstallStepChanged` (runs inside the compiled uninstaller itself, so it covers every uninstall path - the installer's own Uninstall option, Control Panel, and the Start Menu shortcut) to clear `users.json`/`system_settings.json` on uninstall while leaving `instances.json`, mods, and backups alone. NEEDS MANUAL VERIFICATION same as TICKET-0062 for the same sandbox reason - traced the logic by hand and compiled clean, but deliberately did not live-test against the user's actual current install, since that would reset their real admin account outside a moment they chose to do it. Real verification will happen the next time they uninstall/reinstall for real.
* FIXED 2026-07-10 (TICKET-0064): Rewrote `NEXUS_DESCRIPTION.md`'s setup instructions to match `GETTING_STARTED.md`'s full numbered walkthrough, converted to Nexus BBCode ([size]/[b] headers, [quote] for GitHub-alert-style tips/notes/warnings, [list] for bullets, [img] with `PASTE-NEXUS-IMAGE-URL: <filename>` placeholders since Nexus can't embed relative repo image paths). Kept the Nexus-only sections `GETTING_STARTED.md` never had (features, security, requirements, limitations, support, credits, changelog). The two files are now meant to stay in lockstep - a future real content edit to one's setup steps should get mirrored into the other.
* RELEASED 2026-07-10 (TICKET-0065): Version 1.0.2 tagged and published as a GitHub Release with `PalworldServerAdmin-Setup.exe` attached (SHA256 `50f9f4615efc6fa34239169fbde1f08cb8e02041df7354e4e8b0abbb34c5794b`), covering everything on `main` from the `1.0.0` tag through TICKET-0064. Did a full rebuild (frontend + PyInstaller + Inno), not just a fresh installer compile against old `dist\` output, since TICKET-0061 had changed backend Python source the previously-built exe predated - worth remembering for any future release: always rebuild the PyInstaller exe when Python source changed since the last build, not just the installer wrapper around it.
* Real Palworld server console log content will likely never be capturable (TICKET-0019/0020/0023): confirmed the console is rendered via Dear ImGui (`ImGui.ini` found in the server's own config), a GPU overlay with no real text buffer to read - not just "hard to pipe," genuinely not text at all. The Logs page is now real, with AutoPalExpress output from `backend.log` beside this app's own server activity feed, but not Palworld's own engine-window text.
* FIXED 2026-07-10 (TICKET-0066): Added v1 multi-language support - TopBar dropdown (flag + native name), English/Chinese Simplified/Japanese/German/French/Spanish, persisted per user account server-side (survives across logins/devices, not `localStorage`-only). Covers the Sidebar nav and TopBar chrome for v1; most page content is still English-only pending follow-up tickets extending `web/src/i18n/locales/*.json`. Verified the real persistence path via `curl` against the live dev backend (register, set language, fresh login session still shows it, invalid code rejected 400); NEEDS MANUAL VERIFICATION for the actual dropdown click-through in a real browser, since this sandbox can't drive an interactive desktop browser session (same limitation as TICKET-0018/TICKET-0062) - both dev servers were left running for the user to check directly.
* FIXED 2026-07-10 (TICKET-0067): Extended i18n to World Settings - all 99 curated fields' labels/help/descriptions/dropdown options now translate into all 6 languages, while the dropdown `value` written into `PalWorldSettings.ini` always stays Palworld's exact English string (verified live: set `Difficulty=Hard` through the real API and confirmed the literal English string is what's on disk in the real server's `.ini`, not a translated one). Added 22 missing curated English labels to `app/services/palworld_settings.py`'s `_ADVANCED_META` along the way. Found that `uvicorn --reload` didn't reliably pick up every edit to `palworld_settings.py` mid-session - a full dev-server restart was needed before the new labels actually showed up; worth remembering next time a backend edit "isn't taking effect" against a long-running `--reload` process. NEEDS MANUAL VERIFICATION for visually confirming all 6 languages render/wrap correctly across World Settings in a real browser (same sandbox limitation as above).
* FIXED 2026-07-10 (TICKET-0068): The user pointed out only World Settings had actually been translated - extended i18n to every remaining page and panel (Dashboard/roster, Mods/Nexus/UE4SS, Server Control, Launcher Options, Logs, Settings and all its panels, Super Admin and all its panels), so the app is now fully translated in all 6 languages, not just World Settings/TopBar/Sidebar. Also fixed a gap from TICKET-0066: the server run-state word ("online"/"offline") shown by `CrystalStatus` in Dashboard and TopBar was still hardcoded English. Deliberately left English: Palworld launch-flag names (real CLI args) and the Logs page's live activity-feed text (generated server-side, not app copy). NEEDS MANUAL VERIFICATION for visually confirming every page in all 6 languages in a real browser (same sandbox limitation as TICKET-0067).
* FIXED 2026-07-10 (TICKET-0070): Gave the Sidebar's super-admin-only nav items a "premium" visual treatment (gold crown badge, warmer styling, "Host Controls" divider) per the user's request, so the super admin can visually distinguish which pages are exclusive to them - regular admins see no change (those items were already hidden/route-guarded for them).
* FIXED 2026-07-10 (TICKET-0071): Super Admin's Local API "REST API Enabled" toggle now shows its field name as a title above the box with Enable/Disable text inside, matching World Settings' boolean fields (TICKET-0058's layout) instead of showing the field name inside the toggle.
* FIXED 2026-07-10 (TICKET-0069): Implemented the Steam query port option that was previously just researched - per-instance `queryPort` in `instances.json` (default = that instance's own unique `gamePort`), passed to Palworld via `-queryport=`, editable in Super Admin, and covered by the same firewall/UPnP forwarding as the game port when set to a different value. Verified live against the real `Tester1` instance (migration backfill, persistence via real HTTP calls, super-admin gating).
* FIXED 2026-07-10 (TICKET-0073): User feedback right after testing 1.0.3 - the Sidebar's "Host Controls" divider was too subtle to notice (fixed into a solid gold badge), and the Steam Query Port editor moved from Super Admin to Launcher Options (where the other launch arguments live) per the user's request, with Super Admin now showing it read-only.
* FIXED 2026-07-10 (TICKET-0074): Added a "Run Diagnostics" button to Super Admin that runs the existing `diagnose-autopalexpress.ps1` tool in-app (same UAC-elevation pattern as firewall rule changes) and shows the report inline. Found and fixed a real bug while verifying: the diagnostics report file is UTF-16 LE (PowerShell's `Tee-Object`/`Out-File` default), not UTF-8 - reading it as UTF-8 silently produced corrupted text. NEEDS MANUAL VERIFICATION for clicking through the actual UAC prompt end-to-end, since this sandbox can't drive the interactive desktop session and a live elevation prompt was deliberately not triggered on the user's real desktop from an unattended verification step.
* FIXED 2026-07-11 (TICKET-0075): Steam Query Port must never match the Palworld game port. The user confirmed the real behavior live: when `-queryport` equals `-port`, Steam query can bind the UDP port first and Palworld starts the game server on the next open port (e.g. configured 8213, console says 8214). AutoPalExpress now migrates old same-port/default query values to a distinct nearby port, blocks saving a query port that matches any registered server game port, launches with both `-port` and safe `-queryport`, and keeps Super Admin firewall/UPnP steps aware of the separate query port.
* FIXED 2026-07-11 (TICKET-0076): The in-app diagnostics button no longer fails outright when the Windows UAC/elevation prompt is blocked or declined. `diagnostics.run()` now tries the elevated PowerShell helper first, then falls back to running the same diagnostics script without admin rights and prepends a warning that firewall inspection may be incomplete. Verified the fallback path by monkeypatching `_run_elevated` to return false and confirming a report is returned inline.
* FIXED 2026-07-11 (TICKET-0077): Rebuilt the packaged executable and Inno installer after TICKET-0075/TICKET-0076. Current installer checksum is `D07A9649E10605DFB8CDD301AB07778B88EEAE7AB2D7E9039373C1F4F620C91C`.
* FIXED 2026-07-11 (TICKET-0078): Steam Query Port is now opt-in instead of always launched. Added per-instance `useQueryPort` (default `false` for existing and new servers); `process_manager` only appends `-queryport=<port>` when enabled, while keeping the distinct-port safety checks for enabled use. Launcher Options shows an Enable/Disable toggle and grays out the port value when disabled; Super Admin only shows/adds firewall/UPnP query-port steps when the option is enabled.
* FIXED 2026-07-11 (TICKET-0079): Rebuilt the packaged executable and Inno installer after TICKET-0078. Current installer checksum is `57404DC5DAE6FA3BEDF9E5B5D8584AC1AAA94B01DFDF1A8506A44F0E1DAE7322`.
* RELEASED 2026-07-11 (TICKET-0080): Version 1.0.4 - restored `CHANGELOG.md`'s `## 1.0.3` section to match what the real `v1.0.3` tag actually shipped (TICKET-0073 through TICKET-0078 had been getting appended to it after the fact), and added a proper `## 1.0.4` section for TICKET-0073/0074/0075/0076/0078, led with the Steam Query Port/game-port collision fix (TICKET-0075) since that was the headline fix requested. Bumped `installer.iss` to 1.0.4, full rebuild (frontend + PyInstaller + Inno). New installer checksum is `5B726B97261CBB18DBA81A4E4AE5261AFDCF4E9A6386F341B9B043833E429CE6`. Worth remembering: when a changelog section keeps getting edited after its version was already tagged/released, it silently diverges from what that release actually contained - check the tag's committed `CHANGELOG.md` (`git show v<version>:CHANGELOG.md`) before trusting the working-tree version when preparing the next release.
* FIXED 2026-07-11 (TICKET-0081): The Logs page's "AutoPalExpress" panel was exposing real client IP addresses (not from `activity_log.jsonl`, which never contains IPs - from uvicorn's own access log, teed into `backend.log`, and shown verbatim; the app binds `0.0.0.0` and is typically port-forwarded, so every request logged a real IP) to any logged-in user, and was cluttered with recurring `/api/server/status`/`/api/logs/streams` polling noise mistaken for "pings" (no code anywhere actually logs literal "ping" entries). Fixed by masking IPv4 literals in `app_log_reader.read_tail()` for non-super-admins (`app/routes/logs.py` now checks the requesting user's role) and adding a `logging.Filter` on the `uvicorn.access` logger in `app/main.py` that silences access-log lines for just those two high-frequency polling routes (requests still work normally - only the log line is dropped). Also fixed both Logs panels to show newest-first (the AutoPalExpress panel was natural file order/oldest-first before; `activity_log.get_all()` was already newest-first) - export still writes chronological order to the downloaded text file.
* FIXED 2026-07-11 (TICKET-0082): User-reported mod install bug - an archive that bundles its full relative game install path (`Pal/Binaries/Win64/Mods/<ModName>/...`, a valid "drop into your game folder" zip layout) got installed as a doubled, non-functional path (`Mods/Pal/Binaries/Win64/Mods/<ModName>/...`) because `mod_installer.extract_and_install()` only ever unwrapped one lone top-level folder and treated `Pal` itself as the mod's name. Added `_detect_game_path_prefix()`, which strips that exact 4-level wrapper (case-insensitive, only if all 4 levels match precisely - bails out untouched otherwise) before the existing single-folder-unwrap rule runs; `peek_archive_name()` (the pre-install confirmation name) does the same detection against the zip's raw entry names so the name shown before install matches what's actually created. Verified directly against three synthetic archive shapes (the reported nested-prefix bug, a normal single-folder mod, a flat no-wrapper mod) - confirmed the fix and no regression on the other two. Already-broken existing installs aren't auto-repaired; affected users need to remove and reinstall.
* FIXED 2026-07-11 (TICKET-0084): Follow-up to TICKET-0082 found by the user's live test - a second archive shape carrying the same bug, `Pal/Binaries/Win64/ue4ss/Mods/<ModName>/...` (an extra `ue4ss` segment some mod authors include, even though AutoPalExpress's real Mods folder is always `Pal/Binaries/Win64/Mods` with no `ue4ss` segment - see `local_config.py`), which TICKET-0082's stricter 4-segment-only check correctly declined to touch and so fell through to the same broken fallback. Generalized `_detect_game_path_prefix()` to match the fixed `Pal/Binaries/Win64/` prefix followed by an *optional* single `ue4ss` segment before the required `Mods` segment, still bailing out untouched the instant any level doesn't match. Verified both nested shapes plus the existing normal/flat regression cases directly.
* FIXED 2026-07-11 (TICKET-0083): Implemented the multi-file Nexus mod picker requested right after the user tested TICKET-0082/0084 and hit a real mod with 2 files (nexusmods.com/palworld/mods/577) with no way to choose. `app/routes/mods.py`'s old `_select_installable_nexus_file()` always silently auto-picked one file (newest Main, falling back to newest non-old, falling back to newest overall) - split into `_installable_nexus_files()` (the full current-file candidate list, Main-first-then-newest) plus a leaner selector that defaults to the same auto-pick or returns an exact match when a `file_id` is passed. New `GET /api/mods/from-nexus/{id}/files` route exposes that list; `install_from_nexus`/`update_mod` both accept an optional `file_id` query param, defaulting to unchanged auto-pick behavior for single-file mods. New `NexusFilePickerDialog.tsx` (mirrors `InstallFromFileDialog.tsx`'s dialog pattern) - `NexusModBrowser.handleInstall()` fetches the file list first and only opens the picker when more than one file exists, otherwise installs directly exactly as before. Verified the backend selection/listing logic directly against a synthetic Main+Optional payload. `update_mod` accepts `file_id` for symmetry but has no picker UI wired to it yet - a future ticket could reuse the same dialog for updates.
* FIXED 2026-07-11 (TICKET-0087): Removed the bundled `support/Diagnose-AutoPalExpress.cmd` batch file after the user reported Nexus Mods' file scanner flags it - `.cmd`/`.bat` files in installers are commonly flagged as a malware vector regardless of content. Deleted the file; `installer.iss`'s Start Menu shortcut now launches `powershell.exe -File "...\diagnose-autopalexpress.ps1"` directly (same shortcut name, same effective behavior, no batch file shipped at all) instead of pointing at the removed `.cmd`. Safe to drop the `.cmd`'s self-elevation relaunch dance since TICKET-0076 already gave the `.ps1` itself a graceful non-admin fallback (used by the in-app Run Diagnostics button too). Updated the script's own non-admin warning message accordingly; confirmed via grep that `GETTING_STARTED.md`/`NEXUS_DESCRIPTION.md`/`README.md` only ever reference the shortcut by its unchanged display name, never the literal filename, so no doc text needed updating.
* RELEASED 2026-07-11 (TICKET-0086): Version 1.0.5 - converted `CHANGELOG.md`'s `## Unreleased` section into a proper `## 1.0.5` release section, ordered per the user's explicit request with the mod-unpacking fix (TICKET-0082/TICKET-0084) and the new multi-file Nexus picker (TICKET-0083) as the first two highlighted entries, ahead of TICKET-0081's Logs page fixes; TICKET-0087's `.cmd` removal was found and folded in mid-release before publishing. Bumped `installer.iss` to 1.0.5. Final installer checksum is `11AB0B83B8230B00A3F4C8B51451CB4DA68C0756867BDC99170F540685236FD9`. Noted but deliberately excluded from this release's commit: an untracked `palworld_mods/` folder and `tickets/closed/TICKET-0085.md` left in the working tree by a separate AI agent session that built the user a personal one-off UE4SS Lua mod for their own local Palworld install (`D:\games\Steam\...`) - unrelated to AutoPalExpress itself, not staged or touched.
* FIXED 2026-07-16 (TICKET-0156): Maintainability refactor of the four hotspots the user named directly - `palworld_settings.py`'s metadata split into `palworld_settings_data.py`; `routes/mods.py` split into a `routes/mods/` package (wishlist/crud/nexus/manual) with business logic extracted into `nexus_mod_service.py`/`manual_mod_service.py`/`mods_shared.py`; `ServerControl.tsx` decomposed into `components/serverControl/*` plus `useShutdownCountdown`/`useServerUpdateJob` hooks (595 -> ~305 lines); every routed page except Dashboard lazy-loaded via `React.lazy`/`Suspense` (main JS bundle ~1.10 MB -> ~712 KB). Added `ruff` (backend) and `prettier` (frontend) formatting/lint gates plus an explicit `typecheck` script, wired into CI as a new `backend-lint` job and additional `frontend` job steps. Pure structural move, no behavior change - verified via the full pytest suite, `ruff check`/`format --check`, and `tsc`/`oxlint`/`prettier --check`/`vite build` at each step. Also fixed a real regression found mid-refactor: ruff's unused-import autofix stripped `LOCAL_API_SETTING_KEYS` from `palworld_settings.py`'s re-export (needed by `routes/server_settings.py`) - restored with an explicit re-export + `noqa`, caught by directly checking `hasattr()` after the autofix rather than trusting tests alone (the existing test suite didn't happen to cover that attribute path).
* RELEASED 2026-07-17 (TICKET-0162/0163): Version 1.0.8 - converted `CHANGELOG.md`'s `## Unreleased` section into a proper `## 1.0.8` release section covering the APE University auto-completion/spotlight upgrade and its two live-testing follow-up rounds, plus everything already queued (TICKET-0156/0157-0159/0161). Kept the "pre-release build" note carried forward from 1.0.7 - mod deployment (Direct Install, Mod Wishlist approval) is still pending Nexus Mods confirming the app's registration; noted per explicit user request. Bumped `app/version.py`/`installer.iss` to 1.0.8, full rebuild (frontend + PyInstaller + Inno), smoke-tested (`/api/health` OK). Installer checksum `C1628A1501CA579C57C783EDF74A989B356D06CAB9F962E13B89824BFCF13E72`. First release actually pushed/tagged/published (`gh release create`) since local `main` had accumulated many unpushed sessions' worth of commits - confirmed scope with the user before pushing given the size of the gap, per the standing "don't auto-push" preference.

---

## Technical Debt

* No automated test suite anywhere in the project - all verification has been manual/live (see memory/tech_stack.md Notes).
* `process_manager`'s command/control tracking is still in-memory only, lost on every backend restart. Status metrics can rediscover matching Palworld processes by server folder, but Stop/Restart still rely on the tracked launcher plus REST cleanup path.
* No confirmed graceful shutdown path for the manual Stop/Restart actions.
* Git exists, but sandboxed Codex may hit "dubious ownership" because the sandbox user differs from the host owner. Check current git behavior before relying on git commands.

---

## Known Issues

* FIXED 2026-07-06 (TICKET-0006, see Decision Log-adjacent ticket_memory entry): Kicking a player completed without error but didn't disconnect them. Root cause found from the user's own server console log (`User id: steam_<SteamID64>`) - `KickPlayer`/`BanPlayer`/`UnBanPlayer` need that `steam_`-prefixed ID form, not the bare numeric ID `ShowPlayers` returns. Confirmed fixed against a real kick.
* A server started/already running before an admin-backend restart will show as "offline" in the tool afterward, even though it may be genuinely still running and REST-reachable - the tool's scheduler (restart/join-leave checks) goes silent for it until it's restarted through the tool.
* FIXED 2026-07-06 (see Decision Log, two entries): `process_manager.start()` was launching servers with the stale `gamePort` stored in `instances.json` instead of whatever was actually live in `PalWorldSettings.ini`. Went through two passes - first just made launch read the ini (matching what `network.py` already did for port-forwarding), then went further at the user's request: World Settings' "Game Port" field is now the one editable place for a server's port, and launch actively enforces + syncs it both ways (writes it into the ini if missing, writes the resolved value back into `instances.json`) instead of just resolving a read. Also fixed a latent bug this surfaced: `palworld_settings._get_field`/`_set_field` couldn't see a key that happened to be the very first field in `OptionSettings=(...)` (real Palworld files never hit this since `Difficulty` is always first, but the new self-heal path did).
* FIXED 2026-07-06 (see Decision Log): found while debugging a real live deployment where a client PC and a new dedicated server machine, both on the same home router, collided over the router's single external-port-8000 UPnP mapping - the client's earlier "Open Remote Access" click was still occupying it, silently redirecting the public IP back to the client. Neither `RemoteAccessPanel` nor `PortForwardPanel` could show/remove a mapping unless the current browser session had just created it (`forwarded` was local-only state). Both panels now ask the router directly what's mapped (`upnp.get_port_mapping`) and can remove any mapping regardless of which machine created it - this is also the general answer to "the router's own UI won't let me remove a UPnP entry."
* Palworld's REST API has no per-player whisper or teleport command, so those two Players-page actions remain frontend-mocked with no real backend equivalent possible without another API.
* The panel is plain HTTP - if port-forwarded for remote friend access, credentials/session cookies travel unencrypted. Mitigated partially by per-IP login rate limiting, but not eliminated (deliberate, informed trade-off - see Decision Log). In progress: user is setting up a DuckDNS domain + Nginx Proxy Manager reverse proxy for real Let's Encrypt HTTPS (see Decision Log's TLS history for why a self-signed cert/CA was reverted before).
* FIXED 2026-07-06 (TICKET-0008/0009/0010/0011, see Decision Log): full security audit found and fixed 4 issues - RCON password readable by any authenticated user (not just super admin), regular admins able to register arbitrary host folders as "instances" and trigger native dialogs, a login timing side-channel revealing valid usernames, and sessions never expiring server-side.
* OPEN, deferred from the same audit: `firewall.py`'s UAC-elevated `.bat` rule-name interpolation is a latent batch-injection risk (not currently exploitable - every caller passes validated int/enum today). `login_throttle.py`'s per-IP rate limiting will become a single shared bucket for all users once the in-progress reverse-proxy (Nginx Proxy Manager) setup is live, since every request will appear to come from the proxy's own IP - needs trusted-proxy `X-Forwarded-For` handling added as part of that migration.
* FIXED 2026-07-06 (TICKET-0013/0014, see Decision Log): second security audit pass, informed by reading Crafty Controller's source for inspiration. `spa_fallback` (serves the built frontend) had no path-containment check of its own - safe only by incidental Starlette URL normalization, confirmed by calling the function directly with a raw traversal string. Added an explicit check. Also closed a gap TICKET-0012 left open: `automation.router` was still reachable by any regular admin via direct API call even though its only UI entry point is now super-admin-only.
* FIXED 2026-07-08 (TICKET-0029): Nexus Browse cards generated the wrong public Nexus URL path and made the manual verified upload path look like a one-click install button. Cards now open `nexusmods.com/palworld/mods/<id>` and show a Super Admin install-file shortcut or a Super Admin-only notice.
* FIXED 2026-07-08 (TICKET-0030): Dashboard CPU/RAM could show 0% or low memory while Task Manager showed active Palworld usage because status only sampled the remembered launcher tree. Status now samples discovered `PalServer.exe`/`PalServer-Win64-Shipping-Cmd.exe` processes inside the selected server folder, and missing REST frame-time metrics render as unavailable instead of `0 ms`.
* FIXED 2026-07-08 (TICKET-0031): New server deployments now keep the AutoPalExpress data `servers` folder as the default, but the super admin can choose another parent folder. The installer first-server flow carries the same optional location into `first_run_seed.json`.
* FIXED 2026-07-08 (TICKET-0032): Settings now has a prominent per-server "Show in Community Server list" toggle. It persists on the instance record and adds Palworld's `-publiclobby` launch argument the next time that server starts.
* FIXED 2026-07-08 (TICKET-0033): Added safe per-server launch options for Palworld's performance flags, optional worker thread count, and JSON log format. Port, players, community listing, and public IP/port remain owned by their existing app flows rather than duplicated.
* FIXED 2026-07-08 (TICKET-0034): The safe launch options from TICKET-0033 now live on World Settings instead of the Settings server-instance cards. They still persist on the active instance and still require a restart to take effect.
* FIXED 2026-07-08 (TICKET-0035): World Settings now gets Launch Options in the same `/api/server-settings` payload as the `.ini` fields, so the panel cannot disappear because a separate active-instance request failed.
* FIXED 2026-07-08 (TICKET-0036): Launcher flags now have their own super-admin-only sidebar page. Community Server listing, performance flags, worker thread override, and JSON log format all live there, and no longer appear in Settings or World Settings.
* FIXED 2026-07-09 (TICKET-0037): Windows startup recovery can now start AutoPalExpress at sign-in and auto-start the active server when the app launches. The installer includes the same option and explains that it helps bring the server back after machine restarts.
* FIXED 2026-07-09 (TICKET-0038): Direct Nexus installs are restored for super admins with a saved Nexus Premium API key. The app still keeps public GraphQL browsing and verified manual file upload as fallback paths.
* FIXED 2026-07-09 (TICKET-0039): Server Instances now deduplicates records by normalized server folder, so reinstalling or re-importing the same Palworld server no longer shows repeated rows. Settings also has per-server actions to switch to it, open the folder in Explorer, unregister it without touching files, or unregister and delete the server folder after it is stopped.
* FIXED 2026-07-09 (TICKET-0040): The installer now treats existing `%LOCALAPPDATA%\PalworldServerAdmin\data` as update/repair mode. It skips first-time server/admin setup pages, removes stale `first_run_seed.json`, and preserves the existing server list and admin account.
* FIXED 2026-07-09 (TICKET-0041): Nexus Browse now keeps Direct Install visible for super admins and explains the saved Premium key requirement. Launcher Flags was renamed Launcher Options and now exposes separate toggles for `-useperfthreads`, `-NoAsyncLoadingThread`, `-UseMultithreadForDS`, and `-publiclobby`. Server-instance dedupe now canonicalizes stored paths, and the per-instance folder action is labeled Browse Files.
* FIXED 2026-07-09 (TICKET-0042): Rebuilt the Windows installer after verifying the current source and `web/dist` already contain Launcher Options. The new installer checksum is `DE47FB088842FA3135856407919601A759B642BFADF328CCED007B7E1EC2042B`.
* FIXED 2026-07-09 (TICKET-0043): Server Control can now check SteamCMD build ids for the active Palworld Dedicated Server, ask before updating when Steam has a newer public build, and run the stopped-server update as a pollable SteamCMD job. The rebuilt installer checksum is `3C1F74E39D9DC3DFF7BDD5532ACE2DA64A3C11E361036138E570DAEEBAA77F0E`.
* FIXED 2026-07-09 (TICKET-0044): Launcher Options now has super-admin-only toggles for Palworld's `-publicip` and `-publicport` community-listing overrides. The override values are displayed read-only and sourced from the existing Super Admin public-address/game-port flow, so IP and port remain owned in one place. The rebuilt installer checksum is `BB415DF6CFD8163FF406C7C1C023CCF1F3C69E7B510BE43E369CE4A70EE88AE2`.
* FIXED 2026-07-09 (TICKET-0045): Launcher Options' `-publicport` read-only field now uses the live effective game port from the active server's `PalWorldSettings.ini` instead of falling back to the originally stored instance port. Network status also syncs stale stored ports when it resolves a different live port. The rebuilt installer checksum is `4849C5BD3A46DFE7A6C5CC873C7172F7157815C0C461E8F5FD71D1780F1EA216`.
* FIXED 2026-07-09 (TICKET-0046): Reinstall/update port memory now treats the stored Super Admin game port as authoritative once it is custom, enforces that port back into `PalWorldSettings.ini` on launch, and lets older/default instances adopt a custom ini port only when the stored port is still `8211`. The rebuilt installer checksum is `739F2C22E68B60C5B47D5A6C61367CCFE504AA32E96ABFAC7B6C4C202B472C1C`.
* FIXED 2026-07-09 (TICKET-0047): Dashboard roster/player tracking now recovers when Palworld's REST API is running but the ini credential check is incomplete by falling back to the stored local REST management port and by normalizing REST player payload field names. Blank or wrong admin passwords now surface as real REST auth failures instead of the app claiming REST is not ready. The rebuilt installer checksum is `650B493B8A108A33F9EFFDD25C440DFEFBDC47953A96DE4F553948689180C243`.
* FIXED 2026-07-09 (TICKET-0048): Palworld REST Unauthorized errors caused by an empty `AdminPassword` are now self-healed on launch. Starting a server through AutoPalExpress preserves the live ini, enables REST, preserves any user-set AdminPassword, and generates a random AdminPassword only when the field is missing or blank. The rebuilt installer checksum is `59DC70DB4707C0DBBFFBFF13E9850BF3B59E10111BC18B240904600AD43176F9`.
* FIXED 2026-07-09 (TICKET-0049): Added a release-delta changelog section covering TICKET-0041 through TICKET-0048, rebuilt the final executable/installer, and updated the release checksum to `18D018D9A59C247E36F0CF4CD2622AC75F1E4A6D27FB01F4E2B890993944DC1E`.
* FIXED 2026-07-10 (TICKET-0050): Game-port enforcement now edits the live `PalWorldSettings.ini` when present instead of rebuilding from `DefaultPalWorldSettings.ini`, so remembering/enforcing the Super Admin port no longer risks resetting unrelated world settings.
* FIXED 2026-07-10 (TICKET-0051): Rebuilt the packaged executable and installer after TICKET-0050. Current installer checksum is `1116A3ABE445BC3684415134BCC90FFA819F4FB1948305A426A2F04C74A8E034`.
* FIXED 2026-07-10 (TICKET-0052): Added a packaged `Diagnose AutoPalExpress` support command. It elevates for firewall inspection, checks the active instance, Palworld files, live ports, REST API auth, local listeners, and Windows Firewall, then writes a report under `%LOCALAPPDATA%\PalworldServerAdmin\diagnostics`. Current installer checksum is `C97F70ADD720C577EE694A676E3DA0CCF5DE3B94AA93613FEF276AB611879281`.
* FIXED 2026-07-10 (TICKET-0053): World Settings now returns curated group/help/options metadata for known Palworld settings and the frontend renders grouped sections, help tooltips, and dropdowns for categorical values like Death Penalty, Difficulty, Randomizer Type, Crossplay Platforms, and Log Format. Current installer checksum is `D45988C402A38483B9CE7401A1C09720097B112E9A4F28B61A12A42D10B1FF68`.
* FIXED 2026-07-10 (TICKET-0054): World Settings numeric tooltips now include concrete low/default/high examples, compact toggles align with normal inputs, category groups have stronger alternating banding, and Local API settings moved to a Super Admin-only panel. Current installer checksum is `64C125B9E8EF729B770137FB72959A86C57F95122BD77CE542A92CF8B4986E5A`.
* FIXED 2026-07-10 (TICKET-0055): Added `GETTING_STARTED.md`, a GitHub-ready step-by-step setup guide with screenshot placeholders under `images/getting-started-*`, and linked it from the README Quick Start section. Documentation-only change; no executable rebuild required.
* FIXED 2026-07-10 (TICKET-0056): World Settings category headings no longer render the long horizontal filler line, and boolean fields now use the same label-above-control layout as numeric/dropdown fields so toggle controls align in grouped sections. Frontend build passed; no executable rebuild performed.
* FIXED 2026-07-10 (TICKET-0057): Rewrote `GETTING_STARTED.md` for younger/non-technical users with shorter steps, simpler language, and more picture placeholders. Documentation-only change; no executable rebuild required.
* FIXED 2026-07-10 (TICKET-0058): World Settings boolean fields now keep the real setting name in the header position and show `Enable`/`Disable` inside the toggle box. Frontend build passed; no executable rebuild performed.
* FIXED 2026-07-10 (TICKET-0059): Added the available local Getting Started screenshots under `images/` to git so GitHub can render them. Remaining missing placeholders: `getting-started-02-installer-startup.png`, `getting-started-03-admin-account.png`, `getting-started-04-server-choice.png`, `getting-started-05-install-folder.png`, `getting-started-09-router-port-forward.png`, and `getting-started-14-diagnostics.png`.
* FIXED 2026-07-10 (TICKET-0060): Rebuilt the frontend, PyInstaller executable, and Inno installer so the installed app includes TICKET-0058's World Settings toggle header/text layout. Current installer checksum is `6636805A7374892F3A9EE146CCF08C7461C3FE129BC396FB0C8F83933600B9DF`.
* FIXED 2026-07-10 (TICKET-0061): After a Palworld game update, verified World Settings against the live installed server's own `DefaultPalWorldSettings.ini` (Steam build 24088465) rather than trusting the public docs page alone (confirmed the docs page is an incomplete source - it omits real fields the game itself writes). The schema grew to 119 real fields; curated metadata coverage went from 62 to 103, covering guild management, voice chat range, PvP damage/kill-drop config, stat-point-allocation locks, and respawn penalty tuning. The remaining 16 uncurated fields are deliberately left generic (anti-cheat/internal engine tuning, e.g. `bUseAuth`, `BanListURL`, aim-assist toggles). Also fixed `bHardcore`'s help text, which had gone stale now that Pal permadeath is its own separate `bPalLost` field, and hid the new `PublicIP` ini field from the generic editor (added to `_MANAGED_ELSEWHERE`) since Launcher Options already owns the public IP override - same "one place to edit" precedent as `PublicPort`. Verified live: diffed against the real installed template, called `read_all_settings`/`write_settings` directly against a real configured server's live ini with no exceptions, and confirmed a real write/read round-trip. No executable rebuild performed.
* FIXED 2026-07-10 (TICKET-0062): The installer now opens a Setup Mode page (Install / Update / Uninstall radio buttons) right after Welcome, so the one `PalworldServerAdmin-Setup.exe` can drive uninstall directly instead of only being reachable via the separate Start Menu shortcut. Added a real "is AutoPalExpress currently installed" check via the Inno-managed uninstall registry key (`HKA`) - deliberately distinct from the existing data-folder-based `ExistingSetup` check, since app data is kept after uninstall and would otherwise give a false "still installed" positive. Choosing Uninstall confirms, runs the real uninstaller, and hard-exits the setup process (`ExitProcess`) instead of continuing into the wizard. Compiled clean with a real `ISCC.exe`; confirmed this sandbox genuinely cannot drive/observe the interactive desktop session to click through the new page (window enumeration found and "focused" the real Setup window, but screen capture never reflected it - same limitation already noted for TICKET-0018's installer wizard). A human still needs to click through all three radio choices, especially Uninstall, before shipping.

---

## Future Ideas

* Real domain name -> makes Let's Encrypt or a stable Cloudflare Tunnel practical, removing the plain-HTTP trade-off entirely.
* Full process adoption on backend startup (re-attaching Stop/Restart control to an already-running server, not just status metrics) would further improve the "forgets running state on restart" issue.
* Restore-from-backup UI (backups are already structured per-folder with a `meta.json`, deliberately left as a future extension point when the backup feature was built).
* A real automated test suite, if the project's complexity keeps growing - currently proportionate given the single-developer, live-verification-heavy workflow used so far.

---

## Notes

* The `.claude` memory system and ticket folder are active project records. Git exists, but may need a safe-directory exception from the host user before sandboxed git commands work.
* The distributable installer (`PalworldServerAdmin-Setup.exe`) has been built and verified multiple times this project (silent install/uninstall, real launch, TLS-then-reverted rebuilds) - see memory/decisions.md for the TLS history specifically, since that installer was rebuilt more than once for reasons worth understanding before touching TLS again.
* This project involves a live, real Palworld dedicated server and real players (the developer's own friends) - verification throughout has deliberately used real live testing (real UAC prompts, a real router, a real connected player for RCON) rather than only theoretical/unit testing, but destructive actions against real live sessions (e.g. testing kick/ban) have been deliberately avoided even when live-testing was otherwise appropriate.
* FIXED 2026-07-12 (TICKET-0088): Nexus Support found that REST requests still declared app version 1.0.0 while the installer was 1.0.5 and that the authenticated mod-update endpoint could use the super admin's saved key without a super-admin role check. Version 1.0.6 now matches in both the installer and Nexus request header, and every saved-key/download path is super-admin protected.
* FIXED 2026-07-12 (TICKET-0089): Regular admins can add public Nexus metadata results to a per-server mod wishlist without touching the saved key. Super Admin has a separate Mod Wishlist tab with Approve (keyed download/install) and Deny (remove request); approval is the explicit saved-credential authorization boundary.
* FIXED 2026-07-12 (TICKET-0090): Added an understated PayPal donation control to the sidebar footer using the project's existing icon/CSS system rather than the stock PayPal image. It is icon-only in the compact sidebar, gains a quiet “Support the project” label at full width, uses the supplied NOK donation fields, and omits PayPal's tracking pixel.
* FIXED 2026-07-12 (TICKET-0091): Added public GitHub Release update notifications. A cached backend check compares the latest stable `Kvitekvist/AutoPalExpress` release with the installed version and returns a non-fatal unavailable state offline; the sidebar quietly shows a gold-dot update link only when newer. `app/version.py` now feeds Nexus headers and the UI, while the packaging build fails if `installer.iss` disagrees. No GitHub token, automatic download, or installer execution.
* FIXED 2026-07-12 (TICKET-0092 / GitHub #154): Some Windows 10 hosts served packaged `.js` modules as `text/plain` because Python inherited the OS MIME registry, so only the background rendered and strict browsers blocked the login app. `app/main.py` now registers `.js`/`.mjs` as `application/javascript` before Starlette static mounts; an actual built `/assets/*.js` response was verified with the correct header.
* FIXED 2026-07-12 (TICKET-0093): User-reported first-run bug - the installer's seeded first server can still be deploying (SteamCMD download inside `first_run_setup.apply_seed_if_present()`, fired as a background task at startup) when `desktop_app.py` opens the browser, since that only waits for the port to start listening, not for the seed to finish. The top bar's `InstanceSwitcher` (`web/src/components/layout/TopBar.tsx`) fetched `/api/instances` exactly once on mount with no refetch, so it permanently showed "no servers" for that page load even after the seeded deploy actually succeeded - explaining why the user's manual retry with the same name correctly hit `/api/instances/deploy`'s non-empty-folder check ("already exists") while the switcher kept claiming there were no servers, until a manual page refresh remounted it. Fixed by polling `/api/instances` every 3s (mirroring the existing `useServerStatus` pattern) only while no instance has been found yet, stopping automatically once one appears - no indefinite background polling once the normal state is reached.
* FIXED 2026-07-12 (TICKET-0094): Per a user-annotated screenshot, Mod Wishlist approval (TICKET-0089) moved off a second Super Admin tab onto its own super-admin-gated `/mod-wishlist` sidebar page, first under the Host Controls divider, above Launcher Options. `SuperAdmin.tsx` reverted to its plain single-page layout; the wishlist feature also picked up real translation keys across all 6 locales, having previously only shipped with English `defaultValue` fallbacks.
* FIXED 2026-07-12 (TICKET-0095): Added `scripts/build.bat`, a one-command wrapper around `build_installer.ps1` (frontend build, PyInstaller, Inno Setup), at the user's request and to satisfy this project's standing build-script requirement.
* FIXED 2026-07-12 (TICKET-0096): Refreshed `README.md` for TICKET-0088 through TICKET-0095 - added the sidebar donation link to the feature list and documented `scripts\build.bat` as the recommended way to build the installer. Fetched `origin` and diffed against it first since the user wasn't sure whether they'd edited the README directly on GitHub; found no divergence, so the local copy was already what's live.
* RELEASED 2026-07-12 (TICKET-0097): Version 1.0.6 tagged (`v1.0.6`) and published as a GitHub Release with `PalworldServerAdmin-Setup.exe` attached, covering TICKET-0088 through TICKET-0096. `installer.iss`/`app/version.py` already agreed on 1.0.6 from TICKET-0088, so no version bump was needed - just a full rebuild via `scripts\build.bat` and a checksum update in `CHANGELOG.md`/`README.md`/`installer_output/CHECKSUMS.txt`. Release notes highlight the mod wishlist's Nexus Mods API-key compliance angle per the user's explicit request. Final installer SHA256 `aabaddd84a80676ea753e925d85fea23e4bd5a1ba80444edbd738149e3139b83`.
* FIXED 2026-07-12 (TICKET-0098): Added a `wiki/` folder (plus `wiki/README.md` index) with one article per sidebar page, matching `Sidebar.tsx`'s real nav items/order and written from the actual page components rather than guessed. Each article has a screenshot placeholder at `images/wiki/<page>.png`; created that folder via `.gitkeep` (git doesn't track empty directories) for the user to fill in with real screenshots later. User pre-approved this and the push in advance since they were stepping away.
* FIXED 2026-07-12 (TICKET-0099): `git push` had gone from hanging on a Credential Manager sign-in prompt to failing outright with a real 403 - Git Credential Manager's cached `github.com` login was stale, unrelated to `gh` CLI's still-valid one. `gh auth setup-git` pointed git at `gh`'s working credentials, unblocking pushes without needing an interactive sign-in. Also: TICKET-0098's `wiki/` folder is a normal folder in the main repo's tree, not GitHub's actual Wiki feature (a separate `AutoPalExpress.wiki.git` repo, only visible under the repo's Wiki tab) - the user meant the latter. The repo already had Wikis enabled with a pre-existing Home page and Getting-Started page made directly on GitHub (not through this repo) - read both before adding anything. Cloned the wiki repo and added the same 9 sidebar pages there, using wiki-local `images/<name>.png` screenshot paths to match the existing page's own convention (not the main repo's `images/wiki/`), plus a Home.md navigation index. The in-repo `wiki/` folder was kept as a versioned copy alongside the live wiki - this project already tolerates the same kind of parallel, per-venue doc (`GETTING_STARTED.md` vs. the wiki's own Getting Started page), so this isn't new duplication risk.
* FIXED 2026-07-12 (TICKET-0100): User feedback that the wiki pages were too generic - rewrote all 9 (in-repo `wiki/` copy and the live GitHub Wiki) from "what you can do here" bullet lists into numbered "How do I...?" walkthroughs, each step paired with its own screenshot placeholder naming exactly what to circle/highlight (e.g. "circle the Start Server tile and its Change dropdown"), instead of one generic overview image per page. Used a numbered filename convention (`<page>-01-...png`, `<page>-02-...png`) matching the project's existing `GETTING_STARTED.md`/wiki Getting-Started image naming precedent.
* FIXED 2026-07-12 (TICKET-0101): User supplied all 55 screenshots (from a checklist artifact built off TICKET-0100's naming convention) in `Pictures`; moved them into `images/wiki/` in the main repo and into `AutoPalExpress.wiki.git`'s own `images/` folder, fixing one typo'd filename (`mods-03-install-or-wishlis.png` -> `...wishlist.png`) so it matches what the docs already reference. Removed the now-unneeded `images/wiki/.gitkeep`. Both wiki venues now render real screenshots instead of placeholders.
* FIXED 2026-07-12 (TICKET-0102): Stripped the leftover `*(Screenshot placeholder ...)*` italic caption lines from all 9 pages in both the in-repo `wiki/` copy and the live GitHub Wiki, now that TICKET-0101 put a real image under every one of them.
* FIXED 2026-07-12 (TICKET-0103): `README.md` and `GETTING_STARTED.md` now reference the new `images/wiki/` screenshots and link to the Wiki for deeper per-page docs. Notably fixed `GETTING_STARTED.md`'s long-standing missing `images/getting-started-14-diagnostics.png` (never actually captured, per TICKET-0059's memory) by swapping in the real `images/wiki/super-admin-06-diagnostics.png` and mentioning the in-app Run Diagnostics button (TICKET-0074) alongside the Start Menu shortcut.
* FIXED 2026-07-13 (TICKET-0109): Added a "Pending Install" badge/card on the Mods page for wishlisted-but-not-yet-installed mods, and reverted TICKET-0107's disabled Nexus Connect button per the user's explicit preference - they want to see and click the real "Nexus Login" step themselves even without a confirmed application slug, rather than have the app hide it pending confirmation. See `[[feedback_show_dont_hide_unconfirmed_integrations]]`.
* FIXED 2026-07-13 (TICKET-0108): User caught that an expected "no Nexus connection" error on Super Admin also force-logged them out of AutoPalExpress - `httpClient.ts` treats any 401 outside `/api/auth/*` as a dead session, and two Nexus code paths (TICKET-0106's now-common "not connected" 401, plus Nexus's own 401 forwarded verbatim in four routes) collided with that rule. Remapped both to 400 (`NexusApiError.http_status` added for the general case). Important standing rule for this codebase: HTTP 401 must never be reused for a recoverable non-session condition, since the frontend's logout trigger is a blanket rule on that exact status code.
* FIXED 2026-07-13 (TICKET-0107): Nexus Mods hasn't confirmed AutoPalExpress's real application slug yet, so "Connect via Nexus Mods" would have sent the super admin to a Nexus page for an unregistered app - asked the user what to do about it, they chose to disable Connect with a clear "Pending Nexus Mods Approval" state (`nexus_sso.SLUG_CONFIRMED`) rather than guess a value. Backend `/sso/start` also 409s defensively. Rebuilt and shipped (checksum `BF8A8D31377999031F46EC81A6A1FDEFDBF48880681A812AB135CC63351049D7`). TICKET-0105/TICKET-0106's actual Direct Install functionality remains blocked on Nexus assigning the real slug.
* FIXED 2026-07-13 (TICKET-0106): The user caught, right after updating their live server, that mods still installed/wishlisted fine despite having no real Nexus slug and never reconnecting via SSO - TICKET-0105 had blocked pasting a *new* personal key but never invalidated one already saved from before, so it kept silently authenticating downloads. `nexus_session.get_record()` now self-heals: any `connected: true` record missing a `"via": "sso"` marker (i.e. anything from the old flow) is cleared to disconnected on its very next read. Rebuilt and shipped immediately (installer checksum `CF2529186E265FBF8E8383B16903EDBCCC8A4670E50FCA3E795D197E055A640C`) since the user's live server needed the actual fix, not just the code change. Worth remembering: shipping "remove X" needs to account for existing state that already has X, not just block creating more of it.
* FIXED 2026-07-13 (TICKET-0105): Nexus Mods support's last requirement before approving AutoPalExpress's app registration was to fully remove personal API key usage. Implemented Nexus's real SSO protocol (`wss://sso.nexusmods.com`, confirmed via their own `sso-integration-demo`/`node-nexus-api` sources) in `nexus_sso.py` - the super admin now connects by approving AutoPalExpress on a Nexus Mods tab rather than pasting a key; the key itself still works exactly the same afterward (Direct Install, Wishlist approve unchanged). Also replaced always-`false` update-tracking with a real, keyless GraphQL check (`nexus_client.get_current_versions()`), and moved mod updates onto the existing wishlist approve pipeline instead of a one-click action. `APPLICATION_SLUG` is a placeholder until Nexus confirms the real one - NEEDS MANUAL VERIFICATION for the actual SSO browser flow once that lands, and for the new NexusIntegrationPanel/ModCard/ModWishlistPanel UI in a real browser (this sandbox has no interactive desktop session, per the same limitation noted throughout this file - e.g. TICKET-0018).
* FIXED 2026-07-13 (TICKET-0104): Added Save Import from a real support question - a host with no Steam install on the server machine had no way to move an existing co-op save onto it. New `save_import_service.py` finds world-save folders by `Level.sav`, requires the server offline, and reuses the existing backup store (new `backup_service.backup_before_import()`) to snapshot the current save before replacing it. Verified against a throwaway fixture through the real venv rather than a live Palworld server (none available here); that run caught and fixed a real latent bug in `backup_service` where two backups landing in the same second collided on their timestamp-named folder (`FileExistsError`) - now resolved with a numbered-suffix fallback (`_unique_backup_dest()`), which also protects the pre-existing scheduled/manual `run_backup()` path. NEEDS MANUAL VERIFICATION: the actual browser dialog flow (native folder picker, candidate selection, confirm) and rejecting an import while a real Palworld process is online - no interactive desktop/live Palworld server available in this sandbox.
* FIXED 2026-07-16 (TICKET-0152): A customer's split-Steam-session co-op tool ("Nucleus co-op") got Player 2 AUTH-blocked against a dedicated server. Palworld's `bUseAuth` ini field (forces Steam identity validation per joining player) was already generically editable but uncurated since TICKET-0061 left it unlabeled as "anti-cheat/internal" without confirming its actual effect - now has a proper World Settings label/help text ("Require Steam Authentication") naming this exact use case. Worth remembering: TICKET-0061's "leave genuinely internal fields generic" list wasn't fully researched at the time - worth a second look if any of its other uncurated fields (`BanListURL`, `bActiveUNKO`, aim-assist toggles) ever come up in a similar real user report.
* REVERTED 2026-07-13 (TICKET-0120): User asked to fully roll back Run Silently (TICKET-0116/0117/0118/0119) rather than debug it further - three follow-on fix attempts never reached a confirmed-working state. Used `git revert` on the three isolated commits plus manual cleanup of the remaining TICKET-0116-originated code (system_settings.py, process_manager.py, the deleted RunSilentlyPanel.tsx, README/CHANGELOG). Caught and fixed a real mistake mid-revert: reverting the three commits newest-first left `desktop_app.py` on TICKET-0116's *original* broken version rather than a clean slate, since TICKET-0119 was the commit that had removed that code entirely - a full repo grep for leftover references caught it before shipping. Kept `tickets/closed/TICKET-0116.md` as a marked-reverted historical record with a retrospective for if headless mode is ever requested again, rather than deleting it. Rebuilt and shipped a single-exe installer (checksum `8AAD9B9F2C2F26C0C09647A22A9D071CE041C9296178E21C9DB3F934AF527803`). Lesson: when reverting a *sequence* of commits that each rewrote the same code, always verify the actual resulting file content afterward rather than trusting that "revert the commits that touched it" produces a clean end state - the intermediate commits' own history matters.
* FIXED 2026-07-17 (TICKET-0164): Two live bug reports after 1.0.8 shipped. "Last saved: Never" despite repeatedly pressing Save World was a real, widespread bug: `ServerControl.tsx`'s `handleSave`/`handleStart`/`handleStop`/`handleRestart`/`handleCheckUpdate`/`handleBroadcast` all used `try { ... } finally { ... }` with no `catch` at all (only `handleUpdateServer` had one), and `httpClient.ts` has no global error handler to compensate - so any failed request (most likely the Palworld REST API not being enabled/configured, TICKET-0104-era `PalworldRestNotConfiguredError`) failed completely silently with zero user feedback. Fixed by adding matching `catch`/`notifications.error(...)` blocks to all six handlers, plus locale keys across all 5 non-English translations. Worth remembering as a pattern to check for in any future "I clicked X and nothing happened" report: a missing `catch` block is now a confirmed recurring failure mode in this codebase, not just a one-off. The second report (mods wishlisted for a Steam-imported server never installing) was NOT a new bug - traced to the same already-disclosed, already-known limitation from the 1.0.8 release notes: automated Nexus installs are blocked entirely until Nexus Mods approves AutoPalExpress's SSO application registration (`nexus_sso.py`'s `APPLICATION_SLUG` is still a placeholder, same root cause as TICKET-0105/0107). No code fix possible on this side; workaround is the existing "Install From File" manual-upload panel, which doesn't depend on the Nexus API at all.
* FIXED 2026-07-13 (TICKET-0072, TICKET-0110 through TICKET-0116): User pre-approved a batch of 8 tickets in one go ("make all tickets, i preapprove everything"), implemented and shipped together in one installer (checksum `D6A3132784C7B5DC3F37E5C4A4DB8F655CE52C206CD7CE6AF7464C4B6831FFDA`) - see `memory/ticket_memory.md` for the per-ticket detail. Highlights: password reveal toggle everywhere; Mod File Uploads and Import Save relocated (Mod Wishlist page, Server Instances panel respectively); Remote Access/Share With Friends now skeleton-load instead of popping in, and every Enable/Disable toggle reads a static label; a real bug fix for "Last saved" never updating (the timestamp was computed but never actually stored anywhere `/status` could read back); Dashboard gained dual Palworld-vs-system CPU/RAM bars and three network traffic lights (super-admin-only, since `/api/network/*` turned out to already be `require_super_admin`-gated at the router level - the ticket had wrongly assumed otherwise); and a new Run Silently toggle reusing the exact `CREATE_NO_WINDOW` mechanism TICKET-0019/TICKET-0023 already proved out for Palworld's window, plus a new `GetConsoleWindow`/`ShowWindow` path for AutoPalExpress's own console. NEEDS MANUAL VERIFICATION across the board for anything UI-visual (skeleton shapes, traffic-light colors against real router/firewall states, the dual progress bars, the hidden console windows) - no interactive desktop or live Palworld server in this sandbox, verified instead via `tsc`/`py_compile`, reading the resulting logic carefully, and (for TICKET-0113) a direct round-trip check of the new save-timestamp store.
